{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "sh -c 'p=${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}; git cat-file -e \"$p^{commit}\" 2>/dev/null || git fetch -q --deepen=300 origin 2>/dev/null || git fetch -q --unshallow origin 2>/dev/null; git cat-file -e \"$p^{commit}\" 2>/dev/null || exit 1; git diff --quiet \"$p\" HEAD -- . \":(exclude)*.md\" \":(exclude)docs\" \":(exclude).gitignore\" \":(exclude).claude\" && exit 0 || exit 1'"
}
