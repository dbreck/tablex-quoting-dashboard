# tablex-quoting-dashboard

Internal CPQ + project-tracking dashboard for TableX.

## Architecture

Architecture and flow diagrams live in `docs/diagrams/`.

- [Structure map](docs/diagrams/architecture.md) — top-level folders, key relationships, conventions
- [Routing flow](docs/diagrams/routing-flow.md) — URL → middleware → layout → page
- [Monday sync flow](docs/diagrams/monday-sync-flow.md) — Stage 2 pull/reconcile/push engine
- [Zustand store flow](docs/diagrams/zustand-store-flow.md) — store slices, persist/migrate, consumers
- [Supabase auth flow](docs/diagrams/supabase-auth-flow.md) — middleware, AuthProvider, permission gates
- [Data model flow](docs/diagrams/data-model-flow.md) — static seeds → stores → UI
- [Component composition](docs/diagrams/component-composition.md) — who contains who
- [`architecture.html`](docs/diagrams/architecture.html) — interactive viewer (open in a browser)

To launch a Claude Code session with all diagrams appended to the system prompt, run `./bin/cca` (dashboard-internal work only — it loads ~32k tokens of diagrams; skip it for tablex-site orchestration).

Regenerate with `/arch-map` when the architecture drifts.

## Design system (claude.ai/design bundle)

**Share link for anyone asking for "the design system": https://tablex-design-system.vercel.app/** — a static
Vercel project (`tablex-design-system`, danny@clearph.com hub) built from `ds-live/`, published 9/08; redeploy
after a re-sync (memory [[reference-design-system-hosted-url]]). `docs/TableX Design System/README-START-HERE.md`
explains the folder layout (root-level files are the superseded April generation).

**▶ Working the DS? Start at `docs/TableX Design System/ds-live/SYNC-NOTES.md`.** That directory is a
1:1 local mirror of the live claude.ai/design project (`278dcca1-d9ba-4193-a33e-33374cb52ff5`), synced
2026-07-25 — 50 cards across 7 groups (Brand · Colors · Type · Spacing · Components · Application ·
UI Kit). Serve it with `cd "docs/TableX Design System/ds-live" && python3 serve.py` → http://localhost:4321
(the gallery rescans on every reload). Memory: [[project-design-system-sync]] + [[project-tablex-ds-drift-corrections]].

- **`_ds_bundle.js` is the compiled artifact the cards actually render** — the `.jsx` files are source.
  Editing source alone leaves every rendered card stale. Patch both. Reorders (not just relabels) must
  be applied in the bundle too or props mispair with labels.
- **Pull path:** `DesignSync` is **main-session only** (absent from the subagent registry — a fan-out
  cannot fetch), and design-*system* projects have **no zip export**. Use the app's own
  `OmeletteService/GetFile` RPC from a logged-in tab, then strip the `data-omelette-injected`
  style/script pair it inserts after `<head>`. **Push path:** `finalize_plan` (requires `writes` AND
  `deletes`) → `write_files` with `localPath`, which reads from disk so content never enters context.
- **Design exploration ≠ DS project (Danny 8/10):** new design work is NOT pushed into the DS
  project (token-inefficient). Each exploration gets a **NEW regular claude.ai/design project**
  with the TableX DS set as its design system (e.g. "Accessories" `bcdc58f4-…`). Retrieval from
  those: **zip export** (regular projects have it; DesignSync only sees design-system projects) —
  Danny downloads, we unpack. GetFile RPC = fallback.
- **Fonts/assets are already local** — all 8 Acumin `.otf` + 13 of 15 photos in
  `public/comps/home-full-build/`; the rest in `Design Finals/`. Don't try to fetch them.
- The top-level `preview/`, `ui_kits/website/`, `colors_and_type.css` in that folder are the
  **superseded FreightSans generation** — archaeology. Never merge them with `ds-live`.

The authoritative TableX brand bundle lives at `docs/TableX Design System/Design Finals/` — the upload set for the published claude.ai/design Design System. **DS is published + validated.** Grade claude.ai/design handoffs at the CODE level (grep tokens/fonts/shadows). Home landed as a multi-file JSX bundle at `public/comps/home-full-build/` — nitpick fixes are direct `src/*.jsx` edits there (exception to "never implement design output here"; production impl → `tablex-site`). Content-honesty bans apply (see Standing rulings). State: memory `project-claude-design-ds-published.md` + `project-comps-home-bundle-v2.md`.

- Brand source-of-truth: `Brand-Quick-Guide.png` (6 Pantone swatches + Acumin Pro specimens), `Home-01.png`/`Home-01.jpg` (canonical homepage hi-fi — the `.jpg` is the usable 2.6MB export), `TableX.fig`
- Authoritative master spec: `00-DESIGN-SYSTEM.md` (palette, type, anti-AI-slop block)
- Operator's manual for the claude.ai/design session: `13-SESSION-PLAN.md` — read FIRST before opening claude.ai/design
- Full 86-page wireframe briefs in `05`–`12-PAGE-BRIEFS-*.md` grouped by route group

**Brand palette (Pantone-derived, exact):** Forge `#000000` · Iron `#191919` · Saddle `#75400E` · Moss `#8A8962` · Canvas `#EAE5DE` · **Ember `#F26721`** (single accent, one per fold). Adobe Fonts kit: `juc1jwq` (NOT `ucn5jze` — that's Peregrine).

**Stale & superseded** (do not upload to claude.ai/design): `docs/TableX Design System/DESIGN.md`, all `Homepage v2/v3/v4.html`, all `browse-all*.html`, `Assets/fonts/` (FreightSans era), `colors_and_type.css`. Left in place for archaeology.

## Stack

Next.js 16 (App Router) · React 19 · TypeScript · Tailwind v4 · Supabase (`@supabase/ssr`) · Zustand 5 · Monday.com GraphQL · React Three Fiber · Radix UI · Recharts · Plotly · `@hello-pangea/dnd`

## Commands

```sh
npm run dev      # dev server on :3000 (:3001 when tablex-site holds :3000)
npm run build    # production build
npm run start    # production server
npm run lint     # eslint
```

## Invariants worth preserving

- **Hours are canonical; days are derived** (`hoursToDays = h / 8` rounded to 0.5). Baselines stored in days.
- **Laminate pricing: summary tab of the 2026 sheet is authoritative** (Brian 6/5) — Core = "Wilsonart 2 or More" list, Select = +15%, Luxe = +35% (rounded up within category). **Luxe *patterns* = +35** (Brian's "round up to highest markup in category" rule governs). Detail tabs feed stock/banding fields only. `hasMatchingEdgeband` is OFFER-level (always false for patterns). Full rulings in `src/data/finish-catalog.ts` header.
- **Task IDs** are `${deliverableId}-r${idx}` (seeded) or `nanoid(10)` (user-created). Monday's External ID column must match one of these — never title.
- **Zustand selectors must return stable refs** — derive new arrays/objects inside `useMemo` in the hook body, not in the selector. Otherwise React error #185.
- **Scope (frozen quote) vs Tasks (working reality) are separate layers** — `getScopeStatus()` reads overrides; `computeDeliverableStatus()` rolls up from tasks. Keep these paths distinct.
- **Monday sync is manual only** — no polling, no auto-push. Every sync round is ~10k Monday complexity units.
- **All Monday queries must pass `MONDAY_BOARD_ID`** explicitly (`src/lib/monday/schema.ts`). Token is workspace-scoped; our code must act board-scoped.
- **`DELIVERABLES` in `src/data/project-phase2.ts` is static** — never mutate at runtime. Put mutable fields in `DeliverableOverride` in the store.
- **Permission gates are client-side layouts** (`AuthProvider.isAdmin`, `profile.can_access_proposal`). Middleware only enforces "logged in"; API routes that take privileged actions must re-verify via `createAdminClient()`.

## "ship it" — straight to production (startup mode, 7/25)

**"ship it" / "make it live" / "push it live" = merge to `main`, push, confirm the Vercel production
deploy went green.** No PR, no review gate, no staging hop — `tablex-site` `main` auto-deploys to
prod and Danny wants to see real changes on the real site. Flow: commit on a branch → `git merge
--ff-only` into `main` (a fast-forward means the tree is identical to what was already built, so no
re-build; anything else gets a combined local build first) → `git push origin main` → poll
`vercel ls` until the Production row reads `● Ready` → spot-check live. GitHub's PR-required rule on
`main` gets bypassed on push — that warning is expected here, not a problem. This authorizes
**deploys only**: destructive migrations, DNS/MX, and anything else on the confirm-first list still
get surfaced. Interim rule — revisit when real dev/staging procedures land.
Memory: [[feedback-ship-it-straight-to-prod]].

## Cross-repo orchestration (this dashboard → tablex-site)

This dashboard is the **orchestrator** for tablex-site execution work. The PM tracker (`project_tasks` in Supabase `ofweciopslhrepobqpco`) is the canonical task store. Sibling repo lives at `~/Applications/tablex-site` (repo `TableX-Inc/tablex-site`, Vercel `dbreckxs-projects/tablex-site`).

- **Never hand `tablex-site/docs/architecture/target-state.md` to an executing agent.** It's a spec (end-state), not a plan (next-steps). Specs cause drift.
- **Write per-session execution briefs** at `tablex-site/docs/plans/{slug}.md`. Template: `tablex-site/docs/plans/sprint-2.md`. Required sections: stay-vanilla rules, pre-flight, per-task with subtask + AC tracker IDs verbatim, end-to-end verification, reporting back.
- **Briefs reference tracker IDs verbatim** so the agent can mark `subtasks` / `acceptance_criteria` jsonb without title matching. Never paraphrase task titles.
- **Workflow charter** at `~/.claude/plans/woolly-booping-sonnet.md`. Memory: `feedback-cross-repo-brief-workflow.md`.
- **Stay-vanilla rules accumulate.** Every off-rails event in the executing agent becomes a numbered rule in the next brief's pre-flight.

## Current state (2026-09-14)

**tablex-site `main`=`c4aa44b` (9/15 LATE: edge bands round 2 `2439cfc` deployed `6hg07nqsm`; realism-3 brief committed), prod live. Checkout on `main`; the `~/Applications/tablex-spex-realism` worktree (`codex/spex-realism-2`, == `main`) is still running `next dev --port 3100` for Danny — untracked in the main checkout and to be preserved: `docs/12 x18 2 Rad Corners SHORT side.pdf`, `docs/reallspexstudio3dfilesaredone.zip`, `.playwright-mcp/`; when a checkout is held by another agent, work off `main` in a `git worktree` (worktree + `pnpm install` + copy `.env.local`, dev on :3100, push `branch:main`). Dashboard `main`=`836ee38`.** The prior dated "Current state" chronology (7/23→9/12, 135 KB) is archived verbatim at vault `Topics/TableX Dashboard CLAUDE.md Chronology 2026-07 to 2026-09.md` (local copy `docs/claude-md-archive/`, gitignored) and in git at `4193f33`. **Update rule:** a session adds ONE bullet under "What's live" and edits the owed list — never a new dated `##` block.

**Launch (Brian 9/08, Mark + Jim concurred, Danny confirmed 9/09):** Xero live internally **Mon 9/14** · **tablex.com go-live Mon 9/21, early hours, Danny picks the hour.** Sprint 8 — Launch `sprint-L8kQvX` (9/09→9/21, cutover runbook = `web-cutover-*` tasks) · Sprint 9 — Post-launch `sprint-P9mRtW` (9/22→10/09). Change-order communication hold is LIFTED (Danny told Brian to expect the invoice). Memory [[project-launch-locked-2026-09-09]].

### What's live (newest first — detail lives in the linked memory files)
- **9/15 LATE Edge bands round 2 DEPLOYED + Special-T interrogation DONE (Codex `2439cfc` ff'd `4cbae8b..2439cfc`, 100/100 tests + tsc, Vercel `6hg07nqsm` ● Ready, prod routes 200):** 1.5 mm eased corner + seam + PVC sheen (rough .30 / clearcoat .24), band-colour picker (Matching default, Black for patterns) + **required** underside paint (six K-colours, nothing preselected, 400 `underside_paint_required` on save/share/quote), URL `band`/`paint`, right-drag pan, 1640px caps removed, Artisan HPL knife priced ($36 add-on). Report `docs/reports/2026-09-15-spex-edge-bands-2.md`; one test line on draft TX-2026-0008. **Special-T measured** (three r131, ACES .9, PCFSoft 10 % shadow, photographed softbox HDR, Substance-baked ORM on bases, laminates = colour scan + flat normal + constant rough .66 — the top is NOT the gap): **realism-3 Codex brief `tablex-site/docs/plans/2026-09-15-spex-realism-3.md`** + `docs/plans/assets/2026-09-15-spex-realism-3/` (measurements note, HDR thumb, RC screenshots). Rulings: keep Light/Dark grounds + grid, assume Mark supplies nothing (Blender AO bake on fused meshes, part class from `components()`/feet detection, powder class from names, CC0 HDR). Run NOT started. [[project-specialt-render-interrogation-2026-09-15]]
- **9/15 MIDDAY SpeX realism 2 DEPLOYED (Codex built six commits on `codex/spex-realism-2`; Claude fast-forwarded `5ae3bf2..c43a94a` into `main`, deploy `8mrtz8i6v` ● Ready, prod-verified; STATUS.md record `c6070e9`, deploy `idca82f70`):** camera horizon always level (`StudioCamera` interpolates target/spherical/distance itself, Bounds only measures — never restore `moveTo().lookAt()`) · walnut/black study → full roster (66 laminates, solid surface + Maple maps restored, 40 powders on the CC0 Metal028 micro-normal `public/textures/powder-coat/`, structured vs satin) · real 40 powder swatch photos in the picker · Scene menu = Light/Dark background × Bright Hall/Morning Café/Evening Lounge (`scene-settings.ts`, viewer state only, not URL) · App S-v4 from Mark's pre-drill (`glb-models/17/rc1218qd912-s-v4.glb`, identical B tube, 1¾″ mount offset, 11¾×9×.38 base; v3/v2 kept) · static laminate flush / nesting laminate direct-screw hinges + foam pads / fabric mesh pocket (`tableMechanism`-derived, no new URL field). Focused suite = 82 tests (10 files, `--conditions=react-server`), `scripts/models/verify-app.py` PASS. Open: physical Safari/iOS + phone perf, hardware/cloth estimates, Mark reply. [[project-spex-realism-2-deployed-2026-09-15]]
- **9/15 AM App fixes from Mark's "Though" email (SHIPPED `5e0f503`, deploy `mwi8ek59m` ● Ready, prod-verified):** Rectangular Disc **B = column on the long 12″ edge (render was correct), S = column on the short 9″ edge** — `build-app.py` branches on `-S`, new immutable asset `glb-models/17/rc1218qd912-s-v3.glb` (other six App GLBs byte-identical v2; superseded s-v2 left in the bucket). **App offers NO accessories** — `ACCESSORY_FREE_SERIES` in `accessory-options.ts` (`accessoryFreeReason()`), applied in `accessoriesFor` (section auto-hides, URL `acc=` stripped) AND `excludedReason` (pricing). Test cmd that actually works: `node --conditions=react-server --env-file=.env.local --import tsx --test src/lib/spex/*.test.ts …` (83 pass). Open with Mark: is the S column the identical bent tube (→ top offset +0.25 instead of −1.25)? Reply to Mark not yet sent. [[project-app-disc-s-no-accessories-2026-09-15]]
- **9/14 EVE SpeX accessories DEPLOYED (Codex built, Claude committed/merged/deployed per `tablex-site/docs/plans/2026-09-14-spex-accessories-deployment-handoff.md`; `b7004d6` → merge `9b22910` → docs `e8e6412`, deploy `3es5kjgij` ● Ready):** shared center-power support clearance across native families · Villa one/two-unit + placement + DaisyLink, F/DF grommets, exact kit pricing, URL/stored-spec persistence · laminate/acrylic/mesh modesty panels (photo-derived Black/Silver hinge+clamp) · **21 Exclaim 29-inch originals bundled in `public/models/exclaim/standard-v1/`** (RD 24/30/36 + SQ 2424/3030 stay 18-inch, captioned). 69 tests / tsc / build clean on the merged tree. ⚠ Danny is doing the prod browser smoke himself (Ultra centered power + DaisyLink, Exclaim 60×36 = 29 in, round 30 = 18 in, acrylic hardware, F/DF, View Spec). Safari/device QA + measured hardware dims still open. Records: `STATUS.md` + `docs/reports/2026-09-14-spex-hardware-height-qa.md`.
- **9/14 EVE Home hero = Take 2 "Music Paced" (SHIPPED: tablex-site `main`=`9f3630e`, deploy `ef3zy6f09` ● Ready, live-verified):** three 50 s cuts (16:9 `hero-music-paced.mp4` · 4:5 `-mobile` · 9:16 `-vertical`, crf 25 ≈1.2–1.6 Mbps, AAC kept) on the same `<source media>` aspect chain + opening-frame posters. **Headline is CLOCKED TO THE VIDEO** — `HERO_SCHEDULE` in `HomeHero.tsx` keys each word to a cut (0 · 8.54 · 20.5 · 28.5 · 40.54; full 15-cut map in the comment, ffmpeg-verified against the encode), derived from `video.currentTime` every rAF (wall clock until `playing`), five words = one loop; re-time by editing the numbers. **"Watch with sound"** = 44px ghost speaker at the right end of the CTA row, label slides out from behind the icon on hover/focus; opens the SAME file (cache hit) in a `createPortal` modal, unmuted, native controls, plays once (music seam at the loop is a Codex open item), background pauses while open. Source: `Hero Edits/2026-09-14-Premiere Cuts/`. [[project-home-hero-music-paced-2026-09-14]]
- **9/14 Home hero video = "Form to Space" (SHIPPED: tablex-site `main`=`5654e6a`, deploys `gk098r4he` → `nzloy3klx` (9:16 add, `ab21b46`) ● Ready, live-verified; hero-03 404):** THREE cuts picked by viewport aspect via `<source media>` (no JS — each viewport fetches ONE pair; Playwright-verified on iPhone/Pixel/iPad portrait+landscape/desktop + reduced motion): `(max-aspect-ratio: 2/3)` → 9:16 720×1280 `hero-form-to-space-vertical.mp4` (phones, near-exact fit of the 390×736 box) · other portrait → 4:5 720×900 `-mobile.mp4` (portrait tablets) · landscape → 16:9 1280×720 `hero-form-to-space.mp4` 21 s. 2/3 = midpoint of 0.56 and 0.80. Poster underlayer = `<picture>` with the same media chain, each cut's exact opening frame (`hero-poster{,-mobile,-vertical}.webp`); hero-03.mp4 deleted. Danny's rulings: keep the full-height overlay layout (the mobile README's 4:5 media-area reframe declined), supplied posters over the old Ember-circle photo. Scrims untouched — tune after eyeball. Source packages: `~/Documents/Clear ph/Clients/TableX/Video/Hero Edits/2026-09-14-Form-to-Space-{v2,Mobile}/`. **`df52d76`: hero copy wrapper lost its centered `max-w-[1640px]` — it now shares `.space-container` with the header so wordmark/h1/pills sit on the logo's left edge at every width (was 600 vs 140 at 2560 wide; deploy `fm7prihmt` ● Ready, live-verified).** **`797a87f` phone h1 no longer clips:** below md `text-[clamp(34px,10.8vw,148px)]`, md+ keeps `clamp(64px,9vw,148px)` — sized so the LONGEST word "CONNECTING." + caret fits gutter-to-gutter (measured with Acumin Pro Wide loaded: fits 320→1440). Adding a longer hero word means re-measuring. Deploy `lsvh2th3h` ● Ready.
- **9/12 Newsroom:** Payload News with six layouts (standard/feature/essay/showcase/event/verse), Media on Supabase Storage bucket `payload-media` (S3 adapter), `/news` index + `/news/[slug]`, prev/next rail, full-bleed hero option (`heroStyle` + `heroInk`), News + Events under About in the nav. `src/payload-types.ts` is TRACKED. ⚠ **7 placeholder posts (`scripts/news/seed-newsroom.ts`) must be rewritten by Brian in /admin or unpublished before 9/21; stop running the seed once he authors.** [[project-news-events-2026-09-12]]
- **9/11 September 2026 price book = the ONE book** (`f2b907bc…`, 3,121 items, Core/Select/Luxe + hidden KE_BASE; Jan r1–r4 DELETED). `resolveListCents()` in `price-book-shared.ts` is THE list walk; tier flows from `quotedTier` → finish `priceTier` → `/api/spex/price?tier=`. Codex's App 17 · Artisan 43 · Surge 99 · Solo 11 · Trig 03 · Element 33 3D models merged. ⚠ `docs/TABLEX MASTER PRICING - SEPT 2026.xlsx` is an internal cost master — never commit or publish. [[project-sept-2026-price-book-live]] · [[project-spex-app-artisan-models-2026-09-11]]
- **9/10–9/11 design rounds:** hub + sub-page heroes on real photos (`SectionHero` `image` ground, `HERO_CROPS`), sticky sub-navs mirroring the mega menu, `/resources/sustainability` retired (307), `/resources/cad` on the document layout, browse capped at 3 columns + DS button order (SpeX Ember → Explore → Compare), laminates/powder libraries open DARK, butcher-block grain photos, series hero headline below the h1 via `InkAlign.tsx` (optical left alignment — reuse, never hand-author offsets), `/login` "Sign in to TABLE X." in Bebas Neue with an Ember X (auth layout only).
- **9/09 launch lock + tracker reset:** 14 decisions stamped, 20-20 manufacturer code `TBX` shipped, Cinder discontinued (22 outdoor colors), even $3,000 takes the better freight bracket, no order minimum; Codex's design branch merged.
- **9/08 Self-quote v1 + Find-a-Rep:** list-price quotes from the anonymous cart (real TX number, HMAC `/quote/view/[id]?t=`, `/quote/find`, order-by-PO / dealer claim / desk assign + accept-on-behalf), `/find-rep` from Brian's listing (`rep_territories`, metro beats state). [[project-self-quote-findrep-2026-09-08]] · **Freight + SIF + outdoor powder:** `quotes.ship_to` + book zone table → freight row (charge / quoted-separately / desk override), `GET /api/quote/[id]/sif`, 22 outdoor-rated colors. [[project-freight-sif-outdoor-2026-09-08]]
- **9/06–9/07:** rep-demo findings built (line editing, instant pricing = ONE issue path `quote-issue.ts`, revisions, PO on accept, 30-day validity, tier placeholders) [[project-rep-demo-build-2026-09-06]] · **home fold 3 = scroll-driven 3D SpeX story** + Astra's rival take on `/products#spex-astra` [[project-home-spex-scroll-3d-2026-09-05]] · Codex SpeX realism (`studio2` = configurator default; `?visuals=studio` / `?visuals=classic` escapes).
- **9/04:** SpeX locking casters from VertiGO CAD (`acc_caster-lc.glb`, feet detected from base geometry) [[project-spex-casters-2026-09-04]] · feedback board swept 30 → 8 open [[project-board-sweep-2026-09-04]].
- **9/01–9/02 Brian on-site:** typography experiment, finish inspector + dark/light libraries, four photo heroes, rep-for-dealer quoting (`/rep/quotes/[id]` submit-for-dealer), dashboard `/project/rep-demo` findings page (migration 028). [[project-brian-live-session-2026-09-01]]
- **August:** Look Book stack cards on home [[project-home-lookbook-cards-2026-08-28]] · DaisyLink + Dekko STEP → 12 accessory GLBs, Villa power + grommets in SpeX [[reference-spex-3d-accessory-assets]] · WHERE IT FITS scenes on 13 series pages [[project-where-it-fits-shipped-2026-08-26]] · placeholder remediation [[project-placeholder-audit-2026-08-18]] · `/products/collections` deleted [[project-collections-retired-2026-08-18]] · Pattern #17 policing [[project-pattern17-policing-2026-08-18]] · punch-list + finishes photos [[project-punchlist-finishes-2026-08-14]] · browse two-mode + JSI cards + Top-shape facet [[project-browse-redesign-2026-08-09]] · live-priced accessories + SpeX picker [[project-accessories-2026-08-04]] · SpeX Studio 2.0 visuals [[project-spex-studio-visuals-2]].
- **July:** hub-hero mechanism on all 5 hubs + Trans Shadow browse tiles [[project-hub-hero-sweep-2026-07-30]] · ColorBlock heroes on 14 series pages (do NOT revert) [[project-imagery-taxonomy]] · hub-first mega menu, all-caps pills, video hero [[project-nav-hub-first-megamenu]] · pricing wired book → desk + SpeX, auto-PDF, tier selector, 50/10 tier [[project-pricing-wired-2026-07-23]] · `/ops/pricing` workbench [[project-admin-pricing-2026-07]] · `/resources` real library [[project-resources-library]] · feedback widget + `/ops/users` + CRM + Xero read-only sync.

**Dashboard-side pages:** `/project/rep-demo` (findings check-off + notes) · `/project/launch-status` (content in its `data.ts`) · `/project/launch-timeline` (editable Gantt).

### Owed / pre-launch blockers (verify before acting — some may have moved)
- **SpeX realism-3 (9/15 LATE): brief written, Codex run NOT started** — `tablex-site/docs/plans/2026-09-15-spex-realism-3.md`. Danny starts Codex on `codex/spex-realism-3` off `c4aa44b` (worktree `~/Applications/tablex-spex-realism`, currently on `codex/spex-edge-bands-2` == main code); Claude merges/deploys. Edge bands: both rounds DEPLOYED; Brian may still want the legacy nine-colour PVC roster in the band picker (data swap). [[project-specialt-render-interrogation-2026-09-15]]
- **Codex realism round 2 SHIPPED 9/15 midday** (see What's live). Left open by its reports: physical-device Safari/iOS + production phone performance, representative (not measured) hinge/foam/mesh dimensions, static-vs-folding laminate assembly unverified. SpeX briefs for Codex = outcome-level asks only, no file/coding advice (Danny 9/15).
- **Danny cutover (Sprint 8 `web-cutover-*`, runbook `tablex-site/docs/launch-checklist.md`):** Vercel domains (apex decision) · Adobe Fonts kit `juc1jwq` allowlist tablex.com + www · auth origins + hardcoded invite-template origin → tablex.com · Xero prod redirect URI `https://tablex.com/api/xero/callback` · DNS 9/21 (**DON'T touch MX/M365**) · flip `NEXT_PUBLIC_SITE_LAUNCHED` (robots) · legacy-WP afterlife · demo-auth revert (dealer seed `danny+dealer@clearph.com`, `digital@tablex.com` admin) · recreate Brian/Mark/Richie via /ops/users password mode · runtime `DATABASE_URI` on the transaction pooler (6543) in Vercel, session (5432) locally for migrations.
- **`web-auth-familiar-login` (due 9/18):** password-first /login, real forgot-password → recovery email → self-serve `/auth/set-password` (unbuilt), change password in the account; magic link demoted.
- **Brian:** rewrite/unpublish the 7 placeholder news posts · Sept-book confirmations (Gmail draft `r8109522311884990687`: does the increase hit accessories/options/freight, Ultra D-shaped rows, Artisan 42×60/42×84, App `-B`/`-S`) · Southern NJ rep JMA vs CFM (routed JMA) · third-party-marks sign-offs (OSU custom-graphics shots, Pantone/marker branding in the desk hero, Apple-logo MacBook in Ultra-Workplace) · `manufactur-` stem in his own /about + warranty copy · powder "+*" custom-colors claim · final sign-off.
- **Mark:** Artisan shelf sizes/pricing (DR-only) · caster count vs "set of four" + VertiGO `-LC` still selectable · Foundation CAD 30 ↔ book 01 alias · CAD/book codes 01/02/03/09 unclaimed.
- **Byrne/Dekko (hard blocker):** redistribution rights for the 4 Dekko OEM power-tile renders on `/products/accessories` — asked 8/06, unanswered; contact Conor Regin (NDA signed 8/10). Residual CAD gaps in the conversion report.
- **Send-owed drafts:** Neal casters-only brief `r3126663444885169282` · Brian client-review `r1328118608952568301`.
- **Caleb:** `tablex-site/docs/photography-coverage-gaps.md` #1–4 + unshot WHERE IT FITS rows (Revel/Justice-Training, VertiGo ×2, Solo, Trig ×2).
- **Deferred, not built:** P-8 free-text line type · P-9 dealer-admin role · P-4 expiry reminder (needs a cron) · D-3 dark ground · tier name on PDFs/dealer pages · TableXpress/QS pricing (19/50 map coverage) · sitewide TableXpress rename (`/quick-ship`) · browse P3 remainder (live counts, compare tray) · Collections rebuild (`collections-post-launch.md`) · `/dealer/downloads` on the resources catalog · linen `sampleUrl` hover.
- **Human-gated board cards:** #2 #171 #175 #176 #266 #267 #268 (Danny/Kayla design) · #302–305 #315 #372 #389 #392 (photo-blocked) · #74 #145 answered on-card.

## Standing rulings & recurring gotchas

**Content honesty — hard bans on public surfaces (a comp/DS never overrides these):**
- ZERO manufacturing in Jasper — ships/distributes only (no welders/shop-floor/tours copy). **MADE IN AMERICA IS KEPT (Danny 9/04, confirming Brian's 9/01 on-site reversal `d1b97b6`):** the footer MiA seal (`made-in-america.png`, footer-only, unlinked) + `/about/made-in-america` are LIVE and stay. The 8/21→8/26 "Family Owned & Operated" pivot (`1fbf2a1`, tracker `dec-family-owned`) is HISTORY — do not re-retire the seal, do not scrub MiA copy as a "fix". `/about/family-owned` still exists as a page (unlinked from the footer) — leave it unless Danny says otherwise; "Family owned and operated" remains a true fact usable in copy (e.g. the /about Team glance row). Still true regardless: no welders/shop-floor/tours copy; "manufactur-" stays banned outside Brian's own authored lines; footer keeps "Shipped from Jasper, Indiana". Brian's per-line assembly-vs-origin verification (pending since 7/09) is what would ever reopen this.
- `617` / `12,338` stats banned on public surfaces (ClearPH pitch-deck only).
- "mass-produced" + the `manufactur-` stem are banned (Danny 7/04) — **EXCEPTION 7/22: Brian's own authored card copy carries the stem on /about + /about/warranty** (shipped verbatim, pending his sign-off). Don't scrub those instances, don't write NEW ones.
- "Configurable SKUs" → "Configurations". No fabricated SKUs anywhere; Spex shows a real-catalog **configuration code**, never an invented SKU / price / lead-time — **don't "fix" these as bugs.**
- Never fabricate: per-series marketing copy (bespoke copy is **Foundation-only**; the other 15 series use honest fallbacks — memory `feedback-preserve-comp-copy.md`), imagery (flag greybox `data-swap-later`, never invent), contract #s, download tiles.

**Pricing (superseded 7/23, re-based 9/11):** the site DB price book is the source of truth (NOT Xero, NOT `Tablex_Pricing_6.10.24.xlsx`). LIST prices render publicly (SpeX toolbar, `/products/accessories`, browse "From $X list", `/api/spex/price`); NET pricing renders only on issued quotes (dealer page + email + PDF, same gate) — rep surfaces stay zero-dollar, the public self-quote page never shows net. Tiers: 50/20 standard · 50/20/5 · 50/20/10 · 50/20/15 · list/50 · 50/10 (factor 0.45) — every selector derives from `pricing_tier` enum + `tier-options.ts` + `pricing.ts`. Freight $125 lift gate + $300 oversized skid ≥90"; woodgrain +5% is DEAD. % upcharges never appear on marketing surfaces (tier names only). (Laminate invariant: see Invariants above.)

**Image radii (7/29) — tier chosen by the LAYOUT, not the photo:** full-bleed/column-bleed = 0 · image card floating in whitespace = `rounded-img-card` (20px, DS signature) · dense utility tile (browse, SpeX picker) = `rounded-img-tile` (8px) · swatch/chip = `rounded-sm` · circle crops = `rounded-full`. **Never author a literal radius on an image wrapper** — tokens live in tablex-site `globals.css` `@theme`, reference on `/styleguide`; EmberBand `soft-rect` 36px mask exempt (comp variant). Memory [[project-image-radius-standard]].

**Lenis owns wheel scroll on all marketing pages (since May).** Its content ResizeObserver must watch an element whose box tracks document height — `content: document.body` (fixed 7/29, `21c4f6a`; `<html>` is `h-full` so the default starved it and scroll dead-stopped at a stale limit). Never height-pin `<body>`; scroll-freeze reports = check Lenis limit first; hidden/automation tabs throttle rAF and can't repro. Memory [[project-lenis-scroll-freeze-fix]].

**Sitewide conventions (7/22):** terminal quote+rep sections render `QuoteRepCTA` — never inline doors. The "Your ___" marquee order is Table/Space/Project/Vision/Way everywhere. Display name is **SpeX Studio** (capital X) in all copy; routes/identifiers stay `spex`/`Spex*`. Home TriBlock = intent doors SEE IT / SPEC IT / SELL IT with `hoverLight`.

**Photography taxonomy (Danny 7/25) — the category is chosen by the LAYOUT, not the photo:** **ColorBlock** (product on a solid color field) → EDITORIAL (series cards, index rows, collection cards, editorial bands, series heroes) · **Trans Shadow** (cutout + shadow) → STANDALONE PRODUCT (catalog/browse tiles, spec contexts, configurator tiles, comparison rows) · **Real Shots** (real rooms) → **IN-SITU ONLY, in situ or in a carousel — NEVER to depict a series of tables** · **Materials macro** (edge/finish/joinery/hardware) → spec contexts only, banned from editorial and product-identity slots. **Never author the choice — derive it:** `seriesImage(slug, context)` in `tablex-site/src/lib/series-imagery.ts`. Field mapping: `colorBlockImage`/`laneImage`=ColorBlock · `spexImage`/`configRender`=Trans Shadow · **`heroImage`/`inUseImage`=Real Shots**. GOTCHAS: ColorBlock is **portrait (~0.80)** and most slots are landscape → `object-cover` discards ~40%, so always apply the per-series `colorBlockPos` · **`NoGhostImage` is BELOW-FOLD ONLY** (its own docstring); above the fold its lazy reveal never fires and the image silently never loads — use `Image` + `priority` · `spexImage` is 16/16, `colorBlockImage` only 12/16 (`COLORBLOCK_GAPS`).

**Pattern #17 SCALE + GROUND (codified 8/18, DS pushed + site enforced `5640fc2`):** scale is a
function of ROLE, never taste — the strip **IS** the fold → **marquee** (`clamp(40px,5.6vw,96px)`,
full-bleed, loops, ends on the seam) · the strip sits **INSIDE** a fold, or under ~200px of vertical
room → **compact** (30px `--t-h3-size`, static, centered, no seam) · spec/qualifier line → metadata
13px body 500 · label line → eyebrow 12px/600/+0.18em. Metadata + eyebrow are typographic treatments,
NOT variants of the component. **Ground:** white is canonical; on Iron/Forge bands only the ink flips
— items white, `/` one step quieter than its items (stone-300 under full-white, ~40% white under
muted items), `*` Ember on EVERY ground; photography counts as dark; **never on a Moss/Saddle
structural block** (use an eyebrow label). At most one strip per fold. Memory
[[project-pattern17-policing-2026-08-18]].

**Slash-strip punctuation is a GRAMMAR (Pattern #17, codified 7/25 from Kayla's Figma):** `/` joins items **inside one list** (same kind of thing); `*` divides **one list from the next** (Ember). A looping strip ends on `*` — the seam between cycles is a list boundary. Corollaries: one homogeneous list → no internal `*`; all-dissimilar items → every separator is `*`; a static strip has no seam; **a separator never leads a strip**; never `•` `|` `·` `—`. If an item seems to need its own inner separator it is two items — split it. **Never author separators — derive them:** `slashTokens(groups)` in `tablex-site/src/lib/slash-strip.ts` is the single source (`StatStrip` takes `groups`, not the old index-based `accentBefore`); the DS mirror is `ui_kits/tablex-marketing/StatStrip.jsx` + card `preview/slash-strip-variants.html`. Four scales, sized by role: marquee `clamp(40px,5.6vw,96px)` · compact 30px · metadata 13px · eyebrow 12px/+0.18em. Homogeneous strips legitimately render with **no Ember at all**.

**Design / fidelity:** a claude.ai/design comp port is DONE only at **0 real deltas at BOTH 1920×1080 AND 1280×800** (memory `feedback-comp-assembly-fidelity.md`). **The Ops App Language is /ops-ONLY — portals do NOT adopt it (Danny 7/12); don't re-propose.** Sitewide motion/interaction uses the shared butter primitives (`src/components/motion/*`, `src/lib/use-prefers-reduced-motion.ts`) — extend them, never hand-roll folds/image-reveals/reduced-motion probes. Replicate the comp's layout MECHANISM, not its 1920-resolved values — use the fluid `.text-h1/.text-lede` utilities + `--spacing-container/section` steps. Build agents get comp SCREENSHOTS, not just JSX. `/products/browse` = e-comm utility surface (Forge-hero / checkbox-pill); `/products`, `/finishes`, `/spaces` stay editorial. ~~Nav dropdowns deliberately omitted~~ REVERSED (Danny 7/26): nav carries a **hub-first mega menu** — top-level items still NAVIGATE to hubs on click; hover opens a typographic full-width Canvas panel whose first link is the hub itself ("Products Hub →", Ember arrow = the panel's one Ember); header truly transparent over hero pages, Canvas ~97% + hairline on scroll. Source of truth `tablex-site/src/components/{SiteHeader,NavMegaPanel}.tsx` + `nav-links.ts`.

**Dual-Supabase (critical — never cross them):** TRACKER `ofweciopslhrepobqpco` = PM `project_tasks`, personal account, **default `supabase` MCP**. tablex-site Postgres `sfwegefbgudsgricduat` = TableX-Inc org, **`supabase-tablex` MCP**.

**Tracker write gotchas (`project_tasks`):** status lives in the **`column`** field (reserved word — quote it), not `status`. `deliverable_id` MUST be a real `DELIVERABLES` id (`web-1/2a/2b/2c/3/4/5`, `portal-2`…; Spex → `web-3`). `assignee` FK → `team_members` (Caleb=`UDXcXYLU`; else lowercase first-name). `priority` is NOT NULL (batch INSERT rolls back whole on a missing one). Sprints in the `sprints` table (`status='complete'` to close).

**Spex render path (verification trap):** offers ≡ `MODEL_AVAILABILITY` cross-product → most literal-shape combos are NATIVE (baked GLBs — a native render proves nothing about our procedural code). Procedural fires only in the cross-product holes. Confirm via network: `bases/*.glb` = procedural stand-in, `glb-models/…` = native.

**Seeded logins (tablex-site prod):** admin `danny@clearph.com / tablex-admin-2026!` (→ /ops via /portal) · staff `staff@example.com / tablex-staff-2026!` · dealer `dealer@example.com / tablex-dealer-2026!` · rep `rep@example.com`. Impersonate switcher role colors: a=Ember `#F26721` · s=Moss `#8A8962` · d=Saddle `#75400E` · r=Iron `#191919`. (`dealer@example.com` is an unroutable seed address — its Resend emails always bounce; test dealer email with a real address.)

**Xero:** OAuth app "TableX CRM Sync" (`2b22ea93-5ea9-44c8-98c0-9ca192ccf0de`, danny@clearph.com dev login). Post-Mar-2026 apps get **granular scopes only** (`accounting.invoices.read`, not the retired broad scope). Read-only — **never writes Xero.** Books MID-MIGRATION (Move My Ledger, ~7/17); all open invoices are Dec-2020 artifacts (not owed) — `ar_issued_after` cutoff hides them. Suppliers stay out of CRM. Since `596d746` (7/15) the sync captures IsCustomer/IsSupplier + picked mailing address (POBOX-preferred per Xero's mailing-slot convention) + phone (never fax) into nullable `xero_contacts` columns (migration `xero_contact_type_address_phone`; null = pre-capture unknown, false = Xero says no). Xero sets IsCustomer only once an ACCREC invoice exists — dealers with no invoice history read false. Review queue (`/ops/xero`) has open-AR + Customers filters, Type chips, bulk-ignore (`68eff5c`, `596d746`).

**Verification etiquette (Danny 9/11):** don't Playwright-check every small visual change — edit, tsc, then ASK Danny to eyeball it on his :3000 dev server (say exactly what to look at). Browser automation is for what a human can't see (auth-gated/anon flows, font-load state, viewport sweeps) or when asked. **Recurring tooling gotchas:** ⛔ **Never `pkill -f next-server` / name-based kills** — Danny's `next dev` shares the process name and dies too (9/11, three times). Stop my verification servers by PORT: `kill $(lsof -tiTCP:3100 -sTCP:LISTEN)`; never touch :3000/:3001 unless asked. claude.ai/code model choice binds **PER-MESSAGE** — toggling the selector mid-run does nothing until a message is SENT under the new model (send a short steering note; proven Fable→Sonnet 7/13). Extension text-extraction (`get_page_text`/JS `innerText`) can hit a DLP block on session pages ("[BLOCKED: Cookie/query string data]") — fall back to a screenshot. cloud (claude.ai/code) sessions are **branch-constrained since 7/12** — they push `claude/<slug>` branches, never main; the orchestrator merges, runs the COMBINED build locally before pushing main, executes the reports' prescribed prod marker checks, and diffs report claims vs actual tracker rows (a run can deliver a complete report yet mark zero tracker items — butter run 7/13). Two concurrent runs on one repo work when briefs partition the file surface (named no-touch globs + rebase-before-push + stop-on-foreign-conflict). Local push to tablex-site `main` prints a "pull request" advisory but SUCCEEDS (ref updates). chrome-devtools screenshots only write inside a workspace root; `fullPage` saves to a temp path (Read it back). Lazy-loaded below-fold `next/image` shows the fallback bg in a first screenshot — verify via `evaluate_script` naturalWidth or re-shoot after scroll. `resize_page` (chrome-devtools) reflows; claude-in-chrome `resize_window` does NOT. Dashboard dev = **:3001** when tablex-site holds :3000 (check `lsof -iTCP:3000 -sTCP:LISTEN`). `vercel ls` column-mangles when piped — use `vercel inspect <url>` or match deploy `created` to `git log -1 --format=%cI`.

**History:** full per-session play-by-play lives in vault `Sessions/` + memory files (`~/.claude/projects/.../memory/`). This file holds current state + standing rulings only — don't re-accrete a dated chronology here.

## Recurring gotchas (lifted from the archived chronology — still true)

**Payload / Supabase / DB**
- After ANY Payload collection change: `pnpm generate:types` and COMMIT `src/payload-types.ts` (Vercel never had it → type-check failed).
- `pnpm payload migrate` hangs on the stale `dev` row: `set -a; source .env.local; (printf 'y\n'; sleep 200) | pnpm payload migrate`. Session pooler = 15 clients (Vercel instances hold slots) — retry with sleeps. `next build` needs `experimental.cpus: 4` + Payload `pool.max` 3 build / 4 runtime; `max: 1` DEADLOCKS the build.
- Unpublish via the local API = `update({draft:false, _status:'draft'})` (`draft:true` only adds a version). `useSearchParams` on a static route CSR-bails to the nearest Suspense — put the real content in the fallback.
- `supabase-tablex` MCP "Connection closed" = npx cache missing peer-dep `zod` → `rm -rf ~/.npm/_npx/53c4795544aaa350` + `/mcp` reconnect. Direct-pg fallback `scratchpad/dbq.mjs`. Repo convention: no migration files on disk — the `statements` column IS the record.
- Auth-config PATCHes reach GoTrue with a 3–5 min lag. Supabase↔Vercel connection was deleted (site_url sticky) — env sync to Vercel is MANUAL.

**Vercel / builds / servers**
- **Dashboard Vercel storage (9/14):** the free team's 10 GB Deployment Storage filled with 65 × ~150 MB dashboard builds (58 were `docs: CLAUDE.md` commits). Now: `vercel.json` `ignoreCommand` skips builds when a push touches only `*.md` / `docs/` / `.gitignore` / `.claude` (shows as Canceled — expected); `public/` is 9 MB (homepages/ + hpl/tfl textures deleted, normal maps + comps thumb are WebP) — **anything dropped in `public/` ships in every deployment**, keep big assets in Supabase Storage; retention 7d prod / 1d others (`PATCH /v1/projects/{id}/deployment-expiration`, duration strings like `"1w"`; `deploymentsToKeep` isn't API-settable). Bulk delete = `vercel api /v13/deployments/{id} -X DELETE --dangerously-skip-permissions`.
- Vercel PREVIEW env lacks `DATABASE_URI` → branch previews error; preview = `vercel pull --environment=preview` + `vercel build` + `vercel deploy --prebuilt --scope dbreckxs-projects`. CLI `inspect`/`ls` need `--scope dbreckxs-projects`. Deployment URLs 302 behind Vercel auth — verify on `tablex-site.vercel.app`. GitHub's PR-required warning on push to `main` is expected.
- One `next dev` per checkout (Next 16 lock); prod-mode verification = `pnpm build` + `pnpm start -p 3100` beside dev. Never write route files while `pnpm build` runs. `next build` overwrites `.next` (dev uses `.next/dev`). Turbopack refuses symlinked `node_modules` in worktrees. `eslint src`, not `eslint .` (lints `.vercel/output`); the `Toast.tsx` react-hooks/refs error is pre-existing.
- Never ship a Premiere master video — transcode (`ffmpeg -crf 25 -preset slow -an -movflags +faststart`, ~2.5 Mbps).

**Next / React / CSS**
- React 19 + Compiler lint: no closure mutation during render (derive purely), no `Date.now()` in server components (`withinDays`), no setState in effect bodies, three.js mutation lives in plain-TS engine classes (lazy `useState`, not `useRef`); StrictMode double-invokes `useMemo` — clone before re-parenting meshes, bind clipping planes in `register()`.
- `<Link>` can't nest interactive children (card = div + stretched link + sibling buttons). Tailwind data-variants on server-rendered wrappers don't respond to a client toggle — inline styles. `motion-reduce:transform-none` strips base scales — freeze with `motion-reduce:group-hover:scale-[…]`. A table split across a `Fold` loses column alignment — collapse rows.
- ANY overlay/rail scroll container on a marketing page needs `data-lenis-prevent` (+ body lock, MobileNavDrawer pattern) or Lenis swallows its wheel; the `(spex)` group doesn't mount Lenis so it never shows the bug.
- `naturalWidth` on a srcset image is density-corrected — read `w=` off `currentSrc`. Portrait ColorBlocks covering a 533/630 tile pick the source by HEIGHT (`sizes` ≈ 2× tile width). React splits `{expr}` text nodes in served HTML — grep fragments. `&middot;` evades a `·` grep — grep both.

**SpeX / three.js / 3D assets**
- three.js bakes `material.transparent` into the shader — set transparent ONCE, animate only opacity/depthWrite, or a fade is a hard cut. drei `ContactShadows` with ANY y-offset bakes EMPTY; floor planes must set `layers` explicitly. drei array-form `useGLTF([...])` keys the cache on the tuple — one single-URL `<WarmGltf>` per GLB. Yaw accessories via a PARENT group.
- Series-catalog slugs are UPPERCASE (`ruleSlugsInclude`, never raw `.includes`). `price_book_option_charges.series_code` stores "08 Series" (prefix `.like`). `SHAPE_META`: `TC` = Rectangular, `RT` = Racetrack. ⛔ bucket `33/` GLBs = Fundamental geometry — re-key before pointing Element at them. Casters: bases ending in glide pads get one caster per pad cluster; bakes/thresholds validated by `scripts/audit-base-feet.ts`.
- Accessory GLB convention: meters, Z-up, origin = mating surface (Villa = UNDERSIDE, Cove/grommet covers = top drop-ins), `acc_*` mesh names; companions = only what the SKU ships with. **Vendors get the SHORT spec** — the pipeline conventions are ours. Grommets stay quote-only (book $45 vs p.231 example $89). Dekko blocks `curl` (403); WebFetch retrieves their PDFs.

**Pricing / book**
- Extractor `scripts/pricing/convert-pl-master-xlsx.py` → `data/pricing/2026-09-master/`; regenerate + commit `src/data/pricing/spex-model-map.json` per revision; corrections = `scripts/pricing/apply-*.ts` + a dated `price-corrections-*.md`. Freight: exactly $3,000 takes the better bracket; Zone 5/unlisted/no destination = $0 "quoted separately" row, never omitted. Only Core/Select/Luxe laminates + tier placeholders auto-price; Custom + solid surface wait for the desk. Auto-price and the price route share ONE resolver (`accessory-pricing.ts`, `resolveListCents`) — never fork them.

**DS / claude.ai/design**
- `_ds_bundle.js` AND `_ds_manifest.json` are compiled artifacts — patch both with source; grep the manifest after any `@dsCard` change (three drift incidents); measure card height in a browser. The pane's "Add usage notes" field is app-side only. claude.ai/design and claude.ai/code model choice binds PER MESSAGE. Design-export body copy can fabricate product claims — fold copy comes from the data ledes verbatim. Chrome DLP blocks JS reads of secret inputs — Copy button + `pbpaste`.

**Browser automation / verification**
- Playwright MCP shared tab is signed in as admin (and drained an anon cart once) — anon/auth flows in `browser().newContext()`; `window.confirm` in a server-action form = `browser_handle_dialog`; `browser_run_code_unsafe` has no `require`; screenshot `filename` files vanish — omit it. claude-in-chrome tabs freeze GSAP/CSS transitions (verify inline styles); `resize_window` doesn't reflow (chrome-devtools `resize_page` does); `get_page_text` can hit DLP — screenshot instead.
- `[role="dialog"]` probes false-positive on the SiteHeader mobile menu. Priority heroes + NoGhostImage tiles screenshot EMPTY pre-paint — wait ~2 s and re-shoot. Compare renders empty on plain GET (drive the picker). EmberBand frames 6–8 report `naturalWidth 0` (lazy, not broken). A killed-then-restarted `next start` can lose the port race to the OLD server — `lsof` before trusting a curl.
- Gmail MCP can't fetch attachments — open the message in Chrome. Drive image binaries via the local mount ONLY (the Drive MCP base64 blows context). Mid-turn pasted images live at `~/.claude/image-cache/<session-id>/N.png`. zsh: quote `--include=*.tsx`; unquoted `$VAR` in `for` doesn't split; no `timeout` binary; `sleep N; cmd` chains are blocked (background task + `vercel inspect --wait`). Workflow critique agents can stall ~30 min — read `journal.jsonl`.

**Feedback board / tracker / email**
- Feedback `task_comments` needs `site_id`; `feedback.users` column is `full_name`; file audits through the `feedback-board-filing` skill. Widget status control is identity-keyed (`FEEDBACK_IDENTITY_ALIASES`); kill switch `FEEDBACK_WIDGET_ENABLED`. Brian's on-card comments can be superseded by his own later call rulings (Revel, 7/22) — check Fireflies before acting on a stale card.
- TableX M365 runs INKY PhishFence — own-domain-from-external invites get quarantined; create tablex.com users with a password and share out-of-band. `dealer@example.com` bounces. A `manufactur-` or shop-floor line in DS copy docs is Brian-adjacent, not an agent's to scrub.
