# tablex-quoting-dashboard

Internal CPQ + project-tracking dashboard for TableX.

## Architecture

Architecture and flow diagrams live in `docs/diagrams/`.

- [Structure map](docs/diagrams/architecture.md) — top-level folders, key relationships, conventions
- [Routing flow](docs/diagrams/routing-flow.md) — URL → middleware → layout → page
- [Monday sync flow](docs/diagrams/monday-sync-flow.md) — Stage 2 pull/reconcile/push engine
- [Zustand store flow](docs/diagrams/zustand-store-flow.md) — store slices, persist/migrate, consumers
- [Supabase auth flow](docs/diagrams/supabase-auth-flow.md) — middleware, AuthProvider, permission gates
- [Data model flow](docs/diagrams/data-model-flow.md) — static seeds → stores → UI
- [Component composition](docs/diagrams/component-composition.md) — who contains who
- [`architecture.html`](docs/diagrams/architecture.html) — interactive viewer (open in a browser)

To launch a Claude Code session with all diagrams appended to the system prompt, run `./bin/cca`.

Regenerate with `/arch-map` when the architecture drifts.

## Design system (claude.ai/design bundle)

The authoritative TableX brand bundle lives at `docs/TableX Design System/Design Finals/`. It's the upload set for the published claude.ai/design Design System.

**Status (2026-06-06):** DS published + validated + §8a distinctiveness pass. **Home LANDED** (Kayla-recreation trunk, 5/27), Brian's feedback iterated (6/5), and served as a **multi-file JSX bundle** at `public/comps/home-full-build/` — nitpick fixes are direct `src/*.jsx` edits there (exception to "never implement design output here"; production impl still → `tablex-site`). Grade claude.ai/design handoffs at the CODE level (grep tokens/fonts/shadows). Next page: `/about` (`15-ABOUT-KICKOFF.md`, manufacturing-scrubbed). Content honesty (Brian 6/5): **zero manufacturing in Jasper** (shipping/distribution only — no welders/shop-floor/tours copy ever) and **617/12,338 stats banned on public surfaces** (ClearPH pitch-deck only). State: auto-memory `project-claude-design-ds-published.md` + `project-comps-home-bundle-v2.md`.

- Brand source-of-truth: `Brand-Quick-Guide.png` (6 Pantone swatches + Acumin Pro specimens), `Home-01.png` (canonical homepage hi-fi), `TableX.fig`
- Authoritative master spec: `00-DESIGN-SYSTEM.md` (palette, type, anti-AI-slop block)
- Operator's manual for the claude.ai/design session: `13-SESSION-PLAN.md` — read FIRST before opening claude.ai/design
- Full 86-page wireframe briefs in `05`–`12-PAGE-BRIEFS-*.md` grouped by route group

**Brand palette (Pantone-derived, exact):** Forge `#000000` · Iron `#191919` · Saddle `#75400E` · Moss `#8A8962` · Canvas `#EAE5DE` · **Ember `#F26721`** (single accent, one per fold). Adobe Fonts kit: `juc1jwq` (NOT `ucn5jze` — that's Peregrine).

**Stale & superseded** (do not upload to claude.ai/design): `docs/TableX Design System/DESIGN.md`, all `Homepage v2/v3/v4.html`, all `browse-all*.html`, `Assets/fonts/` (FreightSans era), `colors_and_type.css`. Left in place for archaeology.

## Stack

Next.js 16 (App Router) · React 19 · TypeScript · Tailwind v4 · Supabase (`@supabase/ssr`) · Zustand 5 · Monday.com GraphQL · React Three Fiber · Radix UI · Recharts · Plotly · `@hello-pangea/dnd`

## Commands

```sh
npm run dev      # dev server on :3000
npm run build    # production build
npm run start    # production server
npm run lint     # eslint
```

## Invariants worth preserving

- **Hours are canonical; days are derived** (`hoursToDays = h / 8` rounded to 0.5). Baselines stored in days.
- **Laminate pricing: summary tab of the 2026 sheet is authoritative** (Brian 6/5) — Core = "Wilsonart 2 or More" list, Select = +15%, Luxe = +35% (rounded up within category). **Luxe *patterns* resolved 6/13 to +35** (was +20 as printed on the summary tab; Brian's "round up to highest markup in category" rule governs — receipt-confirmed to Brian to object if intentional). Detail tabs feed stock/banding fields only. `hasMatchingEdgeband` is OFFER-level (always false for patterns). Full rulings in `src/data/finish-catalog.ts` header.
- **Task IDs** are `${deliverableId}-r${idx}` (seeded) or `nanoid(10)` (user-created). Monday's External ID column must match one of these — never title.
- **Zustand selectors must return stable refs** — derive new arrays/objects inside `useMemo` in the hook body, not in the selector. Otherwise React error #185.
- **Scope (frozen quote) vs Tasks (working reality) are separate layers** — `getScopeStatus()` reads overrides; `computeDeliverableStatus()` rolls up from tasks. Keep these paths distinct.
- **Monday sync is manual only** — no polling, no auto-push. Every sync round is ~10k Monday complexity units.
- **All Monday queries must pass `MONDAY_BOARD_ID`** explicitly (`src/lib/monday/schema.ts`). Token is workspace-scoped; our code must act board-scoped.
- **`DELIVERABLES` in `src/data/project-phase2.ts` is static** — never mutate at runtime. Put mutable fields in `DeliverableOverride` in the store.
- **Permission gates are client-side layouts** (`AuthProvider.isAdmin`, `profile.can_access_proposal`). Middleware only enforces "logged in"; API routes that take privileged actions must re-verify via `createAdminClient()`.

## Cross-repo orchestration (this dashboard → tablex-site)

This dashboard is the **orchestrator** for tablex-site execution work. The PM tracker (`project_tasks` in Supabase `ofweciopslhrepobqpco`) is the canonical task store.

- **Never hand `tablex-site/docs/architecture/target-state.md` to an executing agent.** It's a spec (end-state), not a plan (next-steps). Specs cause drift.
- **Write per-session execution briefs** at `tablex-site/docs/plans/{slug}.md`. Template: `tablex-site/docs/plans/sprint-2.md` (2026-05-10). Required sections: stay-vanilla rules, pre-flight, per-task with subtask + AC tracker IDs verbatim, end-to-end verification, reporting back.
- **Briefs reference tracker IDs verbatim** so the agent can mark `subtasks` / `acceptance_criteria` jsonb without title matching. Never paraphrase task titles.
- **Workflow charter** at `~/.claude/plans/woolly-booping-sonnet.md` (2026-05-10). Memory: `feedback-cross-repo-brief-workflow.md`.
- **Tracker access:** the dashboard's tracker is on the **personal Supabase account** (`mcp__supabase__*` after the 2026-05-10 PAT switch). Don't reach for `supabase-tablex` — that's the TableX-Inc org, which only owns the tablex-site Postgres.
- **Stay-vanilla rules accumulate.** Every off-rails event in the executing agent becomes a numbered rule in the next brief's pre-flight. Sprint 2's brief has 8 such rules; do not regress.

### State as of 2026-06-06 (Sprint 4 executed early, from THIS session — Danny authorized direct dev)

- **tablex-site is LIVE at tablex-site.vercel.app**: real homepage (13-fold comp scroll), 16-route skeleton, component library + `/styleguide`, working contact/quote forms. Deploys are GREEN — the "parked infra" assumption is stale; confirm `UnParkVcl4` remnants with Danny, then retire Sprint 4 stay-vanilla rules 1/2/4.
- **Email pipeline done**: tablex.com verified in Resend (domain id `99ce1378-…`, DNS via Cloudflare API — scoped token + RESEND_API_KEY in tablex-site `.env.local`); sender `forms@tablex.com`; submissions → `digital@tablex.com` (override: `CONTACT_FORM_TO` Vercel env). Prod form → delivered email, verified.
- **FIDELITY GATE (load-bearing, TWO-VIEWPORT as of 6/7)**: a claude.design comp port is DONE only when `tablex-site/scripts/fidelity/` audit reports REAL DELTAS: 0 at **BOTH 1920×1080 AND 1280×800** (text-keyed getComputedStyle diff vs the comp + screenshot fold-pair eyeball). 1920-only passing is NOT done — the 6/6 "zero deltas" hid total divergence below 1920 (site had hard-coded the comp's 1920-resolved values where the comp uses fluid clamp()/vh + bespoke @media collapse points). **Build agents get comp fold SCREENSHOTS, not just JSX; the comp beats abstract style rules** (only hard bans survive: MiA, 617/12,338, welders). Memory: `feedback-comp-assembly-fidelity.md`.
- **Fluid type system (6/7, hardened DS)**: tablex-site `globals.css` now has `.text-h1/.text-h2/.text-h3/.text-lede` (the comp's exact clamp() formulas, size/leading/tracking ONLY) + comp's 4-step gutter/section @media steps on `--spacing-container`/`--spacing-section` (140/80/40/22 · 128/96/72/56 at 1280/900/560). New pages USE THESE — never hard-code 1920px-resolved sizes. Comp collapse breakpoints are bespoke per fold — port via `max-[Npx]:` variants, not `sm:/lg:`. Eyebrow/nav line-height tokens are 1.55 (comp inherits body leading).
- **Sprint 5 brief rule candidates**: (1) no constants exported from "use server" modules into client components — use a shared module; (2) `pnpm lint` stays at ZERO (cleared this session); (3) never invent routes — `#` placeholders for unbuilt groups; (4) semantic type utilities must not set color/weight (cascade traps — `.text-lead`/`.text-caption` bugs fixed); (5) check native-dep arch (`pnpm install --force` fixes wrong-arch binaries, e.g. lightningcss x64-on-arm64); (6) fidelity gate is the acceptance criterion for any comp port — at BOTH viewports; (7) replicate the comp's layout MECHANISM, not just its values — two flex-vs-block traps so far (hero `justify-center` vs comp's padding-based placement; wordmark img needed `self-start` because flex-stretch + SVG xMidYMid centered it in a full-width box); (8) use the fluid utilities (`.text-h1/.text-lede` etc.) instead of arbitrary clamp values where they apply.
- **Vercel CLI**: tablex-site lives under team `dbreckxs-projects` (account `dbreckx` = `digital@tablex.com`, NOT personal `dbreck`); `vercel redeploy` needs `--scope dbreckxs-projects`. "Sensitive" Vercel env vars are write-only (can't `env pull` them). **As of 6/9 the CLI is logged in as `danny@clearph.com`** (the hub, now a member of the TableX Pro team) — `vercel switch` to `dbreckxs-projects` for tablex-site. Full cross-service login map: memory `reference-account-matrix.md`.

### State as of 2026-06-09/10 (Sprint 4 ungated build queue executed from THIS session)
- **Sprint 4 = `sprint-ZhcdZw`** (Supabase `ofweciopslhrepobqpco`, default `supabase` MCP), populated via dynamic workflow (26 ordered tasks, 5 tracks). 8 pages built + pushed to tablex-site `main` (auto-deploys prod), all eslint+tsc clean, content-honesty greps clean: `/about`, `/about/warranty`, `/quote`+`/quote/quick`, `/find-rep`+`[slug]`, `/spaces`+`[slug]`(×5), `/resources`+5 subpages, `/about/contracts`. Data port done (`src/data/series.ts`+`finishes.ts`, ported VERBATIM from dashboard — 16 series, laminate invariant preserved). All `in-review` in the tracker.
- **Patterns established this push** (reuse for remaining pages): flagged **greybox placeholders** (`data-swap-later` divs) for any missing photo — NEVER fabricate imagery; `src/data/spaces.ts` is data-driven (lead series DERIVED from series.ts `workspaces` tag, not hardcoded); `src/components/resources/ResourceScaffold.tsx` + `ResourceEmptyState` for asset-less resource subpages (honest empty-states + `/contact` fallback, no fake download tiles); contracts use "to be confirmed" number placeholders (never fabricate contract #s); brief-built pages compose existing `marketing/` + `home/` primitives (TriBlock/BigStat/StatStrip/DualCTA/PullQuotePanel) + inline folds.
- **Per-page verify loop**: `pnpm exec eslint <file>` + `pnpm exec tsc --noEmit` + curl :3017 render-200 + rendered banned-term grep → commit+push `main` → mark tracker `in-review`. (Dev server runs on :3017; headless Chrome screenshot path = `--headless=new --remote-debugging-port=9222` + chrome-devtools MCP.)
- **STILL GATED (Danny to unblock):** 2 design comps (`/finishes` Kayla, `/products` Caleb) → catalog hubs; Brian origin decision + static-vs-Payload call → `/series`+`/news`; Kayla's scrubbed careers brief → `/about/careers`; `UnParkVcl4` infra → Payload models; `s4-assets` photography → swaps all greybox. QA/SEO/301 tail runs after pages land.
- **Workflow-agent gotcha:** the Sprint-4 planner agent miscounted series as 18 (it's **16** — verified by hand; the count was interface field + fn param). Always verify agent-reported counts against the source.

### State as of 2026-06-10 EOD (PUBLIC SITE COMPLETE — supersedes "STILL GATED" above)
- **tablex-site main = `556f211`, tree clean, deploys green. Every public route exists and is verified.** This session (2 dynamic workflows + 4 agents): hubs `/finishes`+`/products` passed the TWO-VIEWPORT fidelity gate (0/0 at 1920+1280, adversarial + screenshot backstop); built `/finishes/solid-surface`+`/finishes/custom` (Brian never got the fabrication email → honest quote-only default) + `/products/collections`; **SEO launch tail** (`df0cb0b`: canonicals ×33 pages, sitemap.ts 49 URLs, robots + `NEXT_PUBLIC_SITE_LAUNCHED` noindex guard — FLIP AT CUTOVER + redeploy, default OG, **70-rule 301 map covering all 202 legacy URLs, adversarially verified**); images (products 16→1.4MB, chips PNG→WebP 12.5→1.4MB); Lighthouse Perf 95–99/BP 100; **Payload News+Pages modeled** (migration applied prod; **`push:false` set — dev was silently push-mode against prod DB**; catalog deliberately NOT modeled = static-for-launch, no dual-truth); mobile QA sweep 38 routes @390 (2 real bugs fixed; 9 cosmetic = greybox-caption artifacts, self-resolve with photos); **chrome reconcile vs Home comp** (footer double-gutter fixed, nav ember active dot added, home re-gated 0/0); nav wired to live routes (Login/Account stay `#` until portals).
- **Fidelity tooling (reusable):** `tmp/fidelity-hubs/capture.js` + `overflow.js` (puppeteer-core, one Chrome per run, workflow-parallel-safe); comp server = `python3 -m http.server 8077` in `tmp/comps-hubs/finishes/` — **that ONE zip dir holds BOTH hub comps + Home** (Products zip = older `p_*` generation, ignore).
- **Launch runbook:** `tablex-site/docs/launch-checklist.md`. Footguns pre-documented: Adobe Fonts kit `juc1jwq` needs tablex.com in its domain allowlist; DNS cutover must NOT touch MX/M365 records; `NEXT_PUBLIC_` env changes need a redeploy.
- **Danny-gated:** send Brian Gmail draft `r1655885163917828953` (custom-fab ruling + `/about` "We make tables"/"crafted" voice question — Danny: leave /about as-is until Brian rules); photography drop `s4-assets` (swaps all greybox + clears the 9 cosmetic QA findings); a11y Ember-contrast/target-size/TriBlock-h3 = leave as-is (Danny 6/10).
- **Tracker (sprint-ZhcdZw):** UnParkVcl4 done · s4-finishes, web-3-r0, web-3-r3, web-5-r0, web-5-r1, web-5-r2, web-4-r0 in-review · web-5-r3 doc done/in-progress (execution at cutover).
- **NEXT ARC (fresh session): PORTALS + SPEX STUDIO** — briefs `Design Finals/09` (Spex), `10` (dealer), `11` (rep), `12` (admin). Core Brian-confirmed requirement: dealer login → tier discount (50/20) + net pricing; list price public. Payload is booted (push:false, migrations-only schema writes).

### State as of 2026-06-12 (PORTALS + SPEX ARC COMPLETE — supersedes "NEXT ARC" above)

- **tablex-site main = `3f9fdc1`, prod green, behaviorally verified.** Portals arc all 3 chunks SHIPPED: dealer portal (6/10, `53fa1d6`) · **Spex Studio MVP** (`2fbe1d6`: 3D configurator w/ code-split three.js, quotes flow draft→submitted, 90-day share links, landing + 16 series configurators) · **rep portal thin + admin ops** (`d24ddd9`: /rep dash/dealers/invite/account, **/ops/users + /ops/orgs — NOT /admin, proven build-collision with Payload's catch-all**, variant-driven portal shell, /portal role resolver) · polish (`310a68b`: (spex) route group drops marketing footer on tool surfaces, configurators indexable at canonical bare URLs + sitemap, /rep/quotes/[id], admin Ops↔portal nav) · email sub (`3f9fdc1`: sales@/reps@ → digital@tablex.com — mailboxes don't exist; flip-back rule in commit msg).
- **Workflow model ruling (standing):** ALL workflow subagents run `model:'opus'` (Opus 4.8); Fable 5 is orchestrator-only. The 6/10 quota burn was agents inheriting `claude-fable-5[1m]`.
- **Supabase Auth SMTP → Resend wired** (`sfwegefbgudsgricduat`, sender `TableX <forms@tablex.com>`, Management API — `smtp_port` must be a STRING). Magic-link + invite emails verified delivered. Launch checklist §7 added: flip auth `site_url` (still vercel.app) + redirect allowlist at cutover.
- **Sprint `sprint-ZhcdZw`: zero buildable dev tasks.** All arc tasks + page builds in-review awaiting Danny. Backlog is human-gated: s4-assets photography (Kayla), s4-mfg-decision (Brian/Arabella), web-5-r3 cutover execution.
- **Open asks:** sales@/reps@ mailbox creation (Brian agenda) · rep brief fiction vs real enums (no editor role, no Tier 10/20) → content reconcile · seeded logins: danny@clearph.com / tablex-admin-2026! (admin → /ops via /portal), dealer@example.com / tablex-dealer-2026!.
- **Session-start economy:** skip `./bin/cca` for tablex-site orchestration sessions (it loads ~32k tokens of dashboard-internal diagrams) — plain `claude` + CLAUDE.md + memory is the right context. Use cca only when building dashboard features.

### State as of 2026-06-13 (Q&A SESSION — real laminate textures + Brian voice/pricing shipped)

- **tablex-site main = `7a19ee4`, both prod deploys ● Ready/green.** Dashboard main = `2375674`.
- **Real full-sheet laminate textures on Spex 3D tops** (`6d6e27d`): replaced the 6/12 chip-tiling fix with photographic Wilsonart sheet scans. **The dashboard had NO laminate textures (flat hex)** — acquired fresh from Wilsonart's `productimage/imagedownload/?id=N` endpoint (ZIP → 2000px `fullsheet_5x12.png`). 63/66 SKUs (1280px WebP, 3.3MB, lazy-loaded); 3 holdouts on chip/hex fallback. `useFinishMaterial.ts` got a sheet branch (`SHEET_INCHES=60`, unclamped — small tops show a seamless sub-window). Full method + name-drift notes: memory `reference-wilsonart-laminate-textures.md`.
- **Brian replied to both 6/12 emails** (were unread): custom-fab = no in-house fab, keep generic (confirms build); About voice = **design/deliver/build** → scrubbed+shipped (`7a19ee4`: home hero/About/careers/Artisan — "tablemaker"→"table company", "crafted"→"well-built"/"Built right", "Craftsmanship"→"Considered detail"). **Receipt-reply draft `r3946889989474093118`** in danny@clearph.com — Danny to send.
- **Luxe patterns +20→+35** shipped BOTH repos (Brian's "round up to highest in category" rule; was the documented ⚠ OPEN item). Invariant notes resolved in finish-catalog.ts + tablex-site finishes.ts + this file's laminate invariant line.
- **Accept pass: 13/25 in-review tasks flipped to `done`** (objective gates: fidelity 0/0, adversarial 301/SEO, Lighthouse, runtime role-matrix smoke, prod behavioral). **12 left in-review for Danny's click-through** (10 content pages + 2 Spex) — guided script at `docs/agendas/2026-06-13-accept-pass-clickthrough.md`; **next session leads with it one-item-at-a-time** (directive at top of MEMORY.md).
- **Flags:** /about pull-quote says "Brian Craig, CEO" but sig = VP Sales & Marketing (left as-is, Danny's call). New backlog task `spex-glb-infra` (Frame/Quad GLB import to light up justice+exclaim). Push to tablex-site main still prints a "pull request" advisory but succeeds.

### State as of 2026-06-18 (Sprint 4 CLOSED, Sprint 5 created + sequenced — PM session, no code shipped)

- **Sprint 4 `sprint-ZhcdZw` → complete** (all 36 done). The 12 accept-pass in-review tasks were accepted on **objective/behavioral verification** (prod 200 at `7a19ee4`; zero banned terms welder/mfg/617/12,338/MiA across 17 routes; 6/13 scrubbed voice live "table company"/"Built right"/"Considered detail"; laminate `.webp` textures serve real files; 16 series + 5 spaces render) — NOT per-item eyeball. 6/13 click-through agenda is moot.
- **Sprint 5 `sprint-FlwnOI` ACTIVE 6/18→7/01, 20 tasks, sequenced via `sort_order`.** Framing: site already shipped, so Sprint 5 = **Claude-Design polish/redesign pass** over existing surfaces + real imagery + launch. **Critical-path constraint = IMAGERY (Caleb `UDXcXYLU`, `s4-assets`), not design.** Waves: W0 unblock (`s4-mfg-decision` OVERDUE / `spex-glb-infra` danny / `s4-assets` caleb) → W1 design imagery-light (web-2a templates first) → W2 design imagery-heavy (gated on s4-assets) → BUILD band (4 NEW `port-web-2a/2b/2c/portal-2`, danny, deliverable web-3, two-viewport fidelity = acceptance) → W3 social + `web-5-r3` launch (terminal).
- **2 tracker fixes:** (a) `spex-glb-infra` + `spex-laminate-textures` had bogus `deliverable_id='web'` → repointed to `web-3` (was breaking Monday sync: "parent deliverable web not found"); (b) `s4-assets` reassigned kayla→Caleb (`UDXcXYLU`). Gotchas now in MEMORY.md ("Tracker write gotchas": deliverable_id must be a real DELIVERABLES id; assignee FK → team_members).
- **Danny's NEXT ACTION: `spex-glb-infra`** (self-contained, zero blockers) while chasing Caleb imagery. A copy-paste kickoff prompt for a fresh session was written (dual-Supabase warning: TARGET `sfwegefbgudsgricduat` via `supabase-tablex` MCP vs TRACKER `ofweciopslhrepobqpco` via default `supabase` MCP; encourages dynamic workflows + opus agent teams; subtasks glb-1..4).
- **Still owed (carried):** Brian receipt-reply draft `r3946889989474093118` (danny@clearph.com); /about CEO→VP title call; `s4-quote` live-submit test + spex 3D click-through never headlessly exercised.

### State as of 2026-06-24/25 (Sprint 5 sequenced + spex-glb-infra storage done + Foundation series-page design prep)

- **Sprint 5 `sprint-FlwnOI` re-sequenced + dated.** All 19 backlog tasks given gap-free `sort_order` 10→200 (the 6/18 wave order was already correct — just renumbered) + due dates spread across the **compressed remaining window** (today 6/24 → 7/01, ~6 weekdays): unblock 6/25–26 → design light 6/26–29 → imagery-heavy + build 6/29–30 → social + launch 7/01. `web-2a-r1`'s stale 4/29 due date fixed. `web-5-r3` kept `in-progress`, order/date only. The mislabeled `EW6cG9TI_r` "Social Media" row (has `completed_at` but sits in backlog) left as-is per Danny.
- **`spex-glb-infra`: glb-1/2/3 DONE, now `in-progress` (3/4).** On TableX Supabase `sfwegefbgudsgricduat` (via `supabase-tablex` MCP): created public buckets `glb-models` + `product-images` (size limits + mime allowlists); migration `glb_models_product_images_public_read` (public-read RLS); uploaded **10 real base GLBs** → `glb-models/bases/` w/ `cache-control: public,max-age=31536000,immutable`; verified public CDN serves 200 + `model/gltf-binary`. **Delivery NOT rewired** — tablex-site still serves bases from `/public/models/bases` (Danny: leave on /public for now). Upload script: scratchpad `upload-bases.mjs` (reads service-role key from tablex-site `.env.local`).
- **glb-4 STILL ASSET-BLOCKED (the honest finding):** Justice(40) uses `FR`/Frame on all shapes — **no Frame base GLB exists anywhere**. Exclaim(71) bases = Q/T/TT/X/GNT; only `TT` has a GLB (already renders). The lone quad asset `x-base-quad_4060.glb` is authoritatively **`QC`/"Quad C-Frame" (Primary 44)** per dashboard `sku-to-model.ts:21` + `test-bases/page.tsx:14` — Exclaim doesn't use QC, so mapping it to Q/X = wrong-geometry stand-in **banned by `config-options.ts` honesty rule** ("never a wrong-geometry stand-in"). Caught before shipping. **NEEDS real Frame + Q/X base GLBs from Caleb/3D.** Full detail in the task's `blocker_reason`. tablex-site Spex loads base-only GLBs from `/public/models/bases` via `BASE_GLB` map in `src/lib/spex/config-options.ts` + `baseGlbUrl()` in `src/components/spex/types.ts`.
- **Brian receipt-reply email `r3946889989474093118` CANCELLED** (Danny 6/24 — proceeding without the 200k-sqft + contract-# info for now). Dropped from open items; draft stays in danny@clearph.com if revived.
- **Caleb's photography is IN (`s4-assets`).** Drive: `TableX/Assets/` (15 series folders: Justice/Foundation/Elite/Ultra/Primary/Trig/Element/Solo/Artisan/Exclaim/Stretch/Vertigo/Revel/Surge/Puddle + `App`), each with shape-variant renders, plus `ColorBlock`/`Real Shots`/`Modesty Panel` style sets. **Asset tiers (verified by eyeball):** `*XL.jpg` = **8000×4500 landscape in-situ room renders → HERO/editorial** (warm, daylight, on-brand — covers the hero, no higher-res needed from Caleb); `*Real*.png` = ~1632–1856 portrait in-situ (Moss/slate color-blocked rooms) → editorial/where-it-fits; `*ColorBlock*.png` = product on bold Ember/Moss color ground → the M3 structural color block; `Square/Rect/Round/TTRect.jpg` = 3000² clean cutout-on-white → product/config slots (place on Canvas/Bone). Foundation set copied locally to **`~/Documents/Clear ph/Clients/TableX/Assets/Foundation`** (35 files).
- **Design arc: Series/Product pages first, Foundation = lead template.** tablex-site `/series/[slug]` = 12 folds w/ `data-swap-later` greybox slots; reuses `src/components/marketing/` primitives (BigStat, StatStrip, TriBlock, DualCTA, PullQuotePanel, SpotlightPanel, EditorialRows, EmberBand); DS tokens in `src/styles/tokens.generated.css` + fluid type in `globals.css`; images → `/public/images/products/`. Added an optional **CONFIGURATIONS** fold (Rect/Square/Round shape strip) to the redesign.
- **Claude Design workflow ruling (this session):** DS is already published → **don't create a new project + upload Home-01.jpg as "north-star" (that's the no-DS workaround).** Better: build the series page **inside the existing "Home - Full Build" CD project** so it reuses real component instances (Spex spotlight, stat strip, featured-collections cards, dual-CTA, footer). Caveats: tell CD "new page, don't modify the home page"; watch context-bloat lag. **Template-from-project feature has been removed (CD is drifting from the April-2026 playbook).** Danny is testing BOTH ways (new project vs in-existing-project) to compare. Final reframed CD first-prompt + curated upload list (`FoundationXL/ColorBlock1/Square/Rect/Real1`) live in this session's vault log.
- **`Home-01.jpg` already exists** at `docs/TableX Design System/Design Finals/Home-01.jpg` (2.6MB, 2960×14999, faithful full homepage) — use it; the `.png` is the 36MB oversized export. (Reference still shows banned-public "617" — fine as a pattern ref; prompt bans reproducing it.)
- **WebP/asset-transfer notes:** no `rclone`/`gdrive`/`gcloud` CLI installed; `cwebp`+`sips` present. Don't WebP the masters or pre-convert for CD (files are under CD's ~30–40MB cap; WebP gives CD zero design benefit). WebP+resize is the **repo port step, per-slot, from originals**. Bulk staging: Danny downloads the Drive `Assets` folder locally → I batch-process.
- **NEXT:** Danny runs the Foundation CD session (both ways), shares the winning comp URL → I port into `/series/[slug]` through the two-viewport fidelity gate (1920+1280), then fan out to the other 15 series + do the `/products` hub. Bulk imagery swap from the local folder download.

### State as of 2026-06-25 (SERIES PAGE REDESIGN + IMAGERY FAN-OUT SHIPPED — supersedes the 6/24/25 "NEXT")
- **tablex-site `main`: Foundation `7dfa88f`, 15-series imagery fan-out `db820e2`** (both auto-deploy prod). `/series/[slug]` fully redesigned + every series carries real photography. Full detail: memory `project-series-page-redesign.md`.
- **Method:** Danny ran the Foundation page in CD **twice** (in the existing "Home - Full Build" project = Comp A, token/fluid → trunk; and a new DS-anchored project = Comp B, 1920px monolith → graft source). Exported both as zips to `dashboard/docs/`. We went **section-by-section**: trunk = Comp A; grafted Comp B's **folds 2 (dark trait strip), 4 (SVG Configurations), 9 (Spex white-card-on-Canvas), 12 (left-aligned Canvas CTA)**. Refinements: header overlay on `/series/*` (shared SiteHeader), 3-line hero, two-paragraph "what it is", finishes chip borders off, big-stat label +30px / 40ch / optical left-align, Spex shadow→hairline + sharp-trimmed render. **No section numbering** (Danny rejected). Pills stay lowercase (DS rule). Where-it-fits + also-in-this-lane keep REAL `/spaces`+`/series` links (Danny: keep, not comp-match).
- **Data-driven for all 16** via extended `src/data/series-detail.ts` (imagery slots + bespoke-copy fields). **Bespoke copy is Foundation-ONLY; the 15 use honest fallbacks — NEVER fabricate per-series marketing copy** (Danny caught raw-data-for-comp-copy swaps twice → memory `feedback-preserve-comp-copy.md`).
- **Imagery:** 54 webp from the **Drive MOUNT** `~/Library/CloudStorage/GoogleDrive-danny@clearph.com/Shared drives/External Share/TableX/Assets` (by shot-type→series, NOT 15 series folders). Processed by a **15-opus-agent dynamic Workflow**. Reusable `tablex-site/scripts/trim-cutout.mjs` (sharp `.trim()` for cutout-on-white spex shots). Coverage gaps → honest greybox (Elite hero+colorblock; Artisan/Justice/Puddle colorblock; Revel in-use).
- **GOTCHAS:** lazy-load makes below-fold `next/image` show the fallback bg in a first screenshot (looked like a broken flat-orange colorblock; image was fine — verify via `evaluate_script` naturalWidth or re-shoot after load); `Photo` sole-child color-block needs `fillParent` not `-z-10`/`behind`; chrome-devtools screenshots only write inside a workspace root.
- **QUEUED for resume (Danny: sit tight):** (1) colorblock crop spot-check (~10 unverified `colorBlockPos`, one-line tunes); (2) `/products` hub imagery (its own comp pass); (3) per-series bespoke copy (needs comps/Brian); (4) coverage-gap shots (Caleb). Confirm `db820e2` Vercel build green.

### State as of 2026-06-26/27 (colorblock spot-check CLOSED + series redesign tracked in Sprint 5)
- **tablex-site `main` = `95f7706`, prod ● Ready/green** (both `db820e2`+`95f7706` confirmed deployed). Dashboard unchanged (only pre-existing `CLAUDE.md` mod).
- **Colorblock crop spot-check DONE (queue item #1 closed).** All 12 series with a `colorBlockImage` eyeballed vs live prod. **11/12 clean.** Only fix: **exclaim `55%→44%`** (`95f7706`) — it's the ONLY portrait studio product shot (rest are landscape in-situ), so the full-bleed band (`h-[clamp(540px,68vw,880px)]`, object-cover) can only reveal ~49–55% of a portrait image's height while the table spans ~60% → **no `object-position` value shows top+feet at ultra-wide**; 44% frames the full tabletop+legs at the worst-case 880px cap (verify crops at vw≥1294 = cap = worst case).
- **Stretch image "mismatch" RESOLVED — not a bug.** All 3 Stretch images are authentic Caleb renders of the round **X-base café-pedestal** config (a real Stretch base option). "Arched" = the **arched T-base legs** of the rect/classroom config, which exists only as a white-bg product shot (`StretchTBaseRectTop.png`) — NO in-situ render. Copy(classroom)↔imagery(café) tension is real but not dishonest. Visual classroom story = needs a NEW in-situ T-base-rect render from Caleb → coverage-gap queue. Left as-is.
- **Sprint 5 tracking reconciled (Danny chose option B):** created **`port-series-redesign`** (deliverable `web-3`, danny, `done` 6/26, sort 145) capturing the whole series-redesign arc — because it diverged from the planned `web-2a-r1`(Kayla Figma)→`port-web-2a`(port) flow (Danny ran Foundation in CD directly). **`port-web-2a` stays `backlog`** for the formal Kayla-designed 8+ template batch (its 0/0 single-comp two-viewport gate was NOT the process used here). Today's exclaim/stretch work = subtasks under the new task, not standalone.
- **Tooling note:** `project_tasks` status is the **`column`** field (reserved word — quote it), not `status`. chrome-devtools screenshots only write inside a workspace root (used `dashboard/tmp/cb`, cleaned). Push to tablex-site `main` prints the "pull request" advisory but succeeds (ref updated).
- **SPEX STUDIO ARC STAGED for next session (Danny's directive):** all 6 active Spex tasks set `in-progress` in Sprint 5, sequenced **infra→design→port** (sort 20-25): `spex-glb-infra`(20, danny) → `web-2c-r0` layout&controls(21) → `web-2c-r1` swatch(22) → `web-2c-r2` mobile-3D(23) → `web-2c-r3` config-to-quote(24) [all kayla design] → `port-web-2c` PORT→tablex-site(25, danny). Left `spex-mvp`+`spex-laminate-textures` **done** (shipped S4, not reopened). **`spex-glb-infra` glb-4 still asset-blocked** (needs real Frame + Exclaim Q/X base GLBs from Caleb/3D — no honest stand-in; `x-base-quad`=QC/Primary-44 geometry). Design tasks need CD comps before `port-web-2c` can build. **OPEN: `portal-3-r2`** (dealer configurator embed) left OUT of sprint — Spex-adjacent but a `portal-3` dealer task; Danny never answered whether to pull it in. Due dates on the web-2c block are stale (6/26-6/30) — re-spread if needed.

### State as of 2026-06-28/29 (SPEX CONFIGURATOR REDESIGN — built, verified, UNCOMMITTED — pick up later)
- **tablex-site working tree on `95f7706` with UNCOMMITTED Spex redesign — NOT pushed.** 5 modified + 2 new files. Danny had to step away mid-review; decision on commit/push to `main` (auto-deploys prod) is PENDING. Full detail: memory `project-spex-configurator-redesign.md`.
- **CD was SKIPPED.** Ran a claude.ai/design pass first (3 variants) — Danny rejected all (numbered config layers = waste; redundant "Your Configuration" panel; off-brand dark-mode + colored 3D bg). Pivoted to direct implementation. The CD-prompt file `docs/agendas/2026-06-28-spex-configurator-cd-prompt.md` is **superseded/moot**.
- **Direction = test-drove the JSI Furniture configurator** (jsifurniture.com Native conf table). ADOPTED: 2-column (big 3D viewer + config rows), **collapsed rows show their value inline** (that IS the running config → no summary panel needed), **pinned bottom toolbar** + **View Spec** panel. DIVERGED: truly-pinned toolbar (JSI's scrolls away above its footer); **Add to quote** = our killer feature, gets the Ember slot JSI lacks; dropped JSI clutter (Design Files / PDF Spec / Price List).
- **Built via dynamic Workflow, all agents Opus 4.8** (Discover ×4 → Build ×1 high-effort → Verify ×3). All green: tsc + full eslint + `next build` pass; honesty + responsive verify PASS.
- **Changes (tablex-site):** NEW `src/components/spex/SpexToolbar.tsx` (pinned bar — LEFT: series+configCode·Share·Reset·Start-custom-quote; RIGHT: lead-time chip·Submit-for-pricing·**Add to quote** Ember·View Spec toggle) · NEW `src/components/spex/SpecSheet.tsx` (slide-up View Spec sheet, out-expo, full-screen mobile, configCode+Copy+lead-time+pricing+spec rows) · `config-options.ts` new `configCode()` · `ConfiguratorClient.tsx` 3-col→2-col(61/39), deleted summary rail+SummaryRow+mobile tabs+anonPrompt · `(spex)/layout.tsx` min-h-dvh · `page.tsx` honest leadTime · `SpexViewer.tsx`+viewerPanel fill. **THEN (post-workflow, by me) full-height app-shell:** ConfiguratorClient root `lg:h-[calc(100dvh-154px)]` (=vh−78 nav−76 toolbar), viewer fills (`lg:aspect-auto lg:h-full`), config col `lg:overflow-y-auto` internal scroll, retired `lg:sticky top-[94px]`. Fixed the grey-gap-above-toolbar Danny flagged.
- **Verified behaviorally** (me, localhost:3000): 2-col + pinned toolbar render; 3D reacts to selections (Round→round top, dims/configCode update); auto-advance preserved; View Spec sheet opens/closes; Add-to-quote anon→saved-local toast; full-height fill works, gap gone.
- **HONESTY CALLS — do NOT "fix" as bugs:** (1) **no fabricated SKU** — `configCode` joins ONLY real catalog codes (series·shape·size·base, e.g. `00-TC-2036-U`), labeled "Configuration code" not SKU; deliberately did NOT replicate the comp's `ULT-RT-3620-29-DW-TM` (invents height digit + finish initials); finish/edge/qty are spec-sheet rows. (2) **lead-time = "Lead time confirmed on your quote"** (honest placeholder — zero quick-ship data in code). (3) **price = quote-only** ("Submit for pricing"), no dollar figure anywhere; dealer tier-fact only.
- **GOTCHAS:** the dark **"N" circle bottom-left = Next.js dev indicator, NOT our UI** (gone in prod build — don't chase it). `claude-in-chrome` `resize_window` doesn't reflow the screenshot capture (stuck 1400×868) → couldn't grab a real mobile shot; mobile was code-reviewed @390 (V2 PASS).
- **PUNCH LIST (next session):** (1) narrow tablet ~640–820px lead-time chip can crowd/wrap — add `whitespace-nowrap` + gate chip to `lg:` (`SpexToolbar.tsx:107`); (2) **`/share/[token]` `SharedConfigView.tsx` still old 3-col** — propagate the 2-col+toolbar+sheet; (3) optional: recenter 3D camera for the taller viewer (table sits a bit high in fill mode) — `SpexCanvas` camera; (4) minor: stale "three-column"/"SummaryRail" comments + unsynced nav78/toolbar76/sheet76 magic numbers. (5) Then: commit+push, mark Sprint 5 `web-2c-r0` (this executed its design half directly, like `port-series-redesign` did for series); `port-web-2c` = the build row.

### State as of 2026-06-30/07-01 (HAIL-MARY push + browse reinvention + spaces imagery + auth nav — tablex-site `main` `20966ff`, prod green)
- **13 commits shipped, all tsc+eslint+`pnpm build` green, honesty-grep clean, pushed to `main` (auto-deploys prod).** Session log: vault `2026-06-30-...-hail-mary-imagery-search-polish.md` + follow-on refinements below. Started from the autonomous brief `tablex-site/docs/plans/hail-mary-2026-06-30.md`.
- **HAIL-MARY (W1/W2/W3):** `d9adfeb` 4 `/products/collections` cards→real in-situ webp (Conference greybox — no boardroom shot); `cde44e4` modesty/nesting `configRender` fold on `/series/[slug]` (Revel/Exclaim/Element/Foundation; Elite excluded=misfiled Element shot); `5701b69`+`2e7eccd` real catalog **`/search`** (was disabled stub) over series+spaces+finishes+routes + header magnifier; `c775024` **`/quick-ship`** rebuilt PageStub→DS bar; `30ee596` **`/contact`** re-chromed off legacy `max-w-7xl`/`text-heading-lg`→DS bar; `ad12afa` **adversarial-verify fixes**. **`/news` held as honest empty-state** (Payload news/pages tables NEVER migrated to prod, push:false, zero content — a query 500s; `web-2b-r1` blocked). Ran an **Opus-4.8 audit+verify Workflow (9 agents)** — confirmed about/resources/finishes/spaces/transactional already at DS bar, caught a real **`/search` FAIL** (butcher-block→`/finishes/custom` which renders nothing→fixed to `/finishes/solid-surface`; chrome→`/finishes` hub) + wording fixes.
- **`/products/browse` REINVENTED** (`9fd190a`→`4e54342`→`131e2ab`→`cacc351`): Danny disliked flat greybox page → adopted the honest parts of `docs/TableX Design System/browse-all v2.html` reference (STRIP its banned content: Made-in-America/617/fake 10-day lead-times/dealer prices). Now: real series photography on all 16 cards (`heroImage??inUse` from series-detail.ts), **Forge stat hero** (16/97/9,000+), **checkbox-pill facets w/ catalog counts** (borderless unselected, ember-fill+white-check selected, centered count-circles), white filter card, **Foundation best-seller spotlight** (unfiltered only), finish **swatch row** (7 real catalog hexes)+"97 finishes" (66 lam+31 powder), Forge "build from scratch" Spex band. **Danny's ruling: browse = e-comm utility surface; DON'T bring this Forge-hero/checkbox-pill treatment to `/products`,`/finishes`,`/spaces` (those stay editorial/enticing).**
- **`/spaces` + `/spaces/[slug]` REAL PHOTOGRAPHY** (`896f2f0`): new `SpaceEntry.image`; 4/5 filled (training→Foundation flip-top, collaborative→Exclaim lounge, café→Stretch, healthcare→Surge clinical-office w/ anatomy chart). **Conference & Meeting stays greybox** (no boardroom scene). Detail hero = full-bleed photo + gradient scrim; lead-series cards pull real series photos.
- **AUTH-AWARE NAV** (`20966ff`): header was showing Login AND Account. Now one-or-the-other via client `getSession()`+`onAuthStateChange` (keeps marketing static, label-only, never a gate): logged-out=`Login | Contact`, logged-in=`Account | Contact | Sign out` (Account→`/portal` resolver; Sign out POST `/auth/signout`). Deviates from DS literal "Login|Account|Contact" per Danny.
- **PRICING RULING:** the only sheet `…/Quoting Docs and Sheets/Tablex_Pricing_6.10.24.xlsx` is **top/component pricing** (tops by shape×size, modesty panels, drilling/nesting/grommet charges) — **NO per-series base pricing** → can't derive an honest "From $X" → **prices stay OFF cards.** Don't revisit without a real base-price source.
- **NAV DROPDOWNS = DELIBERATELY OMITTED** (Danny asked; answered from docs): `03-COMPONENT-PATTERNS.md` GlobalNav **Don'ts** = "No dropdown menus that auto-open on hover." Top-level items link to **hub pages** that ARE the "mega menu" as full pages (`/products` four-door grid etc.). The dashboard **Site Architecture viewer's expand-panels = a reviewing affordance, NOT a runtime spec.** Building megamenus = a conscious reversal (would do click/focus-triggered, not hover).
- **STALE/BANNED IN THE IA DOC:** `src/data/future-site-architecture.ts` About→"Manufacturing / Made in USA, Midwest facility" node VIOLATES the zero-mfg ruling — tablex-site correctly omits it. **Offered to correct the doc; not done.** Also IA lists Products→Accessories + Resources→Case Studies which are NOT built (no honest data).
- **OPEN (Danny to decide):** (1) **mobile nav = non-functional stub** (`#site-mobile-menu` hidden; <900px hamburger opens nothing) — offered to build the drawer; (2) **cart icon → `#`** (no cart system) — leave / point to `/quote` / drop?; (3) **nav CTA buttons** (Request-a-Quote + Find-Your-Rep): IA shows them, DS says "no CTA button", shipped nav has none — undecided.
- **GOTCHAS:** chrome-devtools `fullPage` screenshots save to a temp path (Read them back, not inline); lazy-loaded below-fold `next/image` shows the fallback bg in a first screenshot → verify via `evaluate_script` naturalWidth or re-shoot after scroll; `resize_page` (chrome-devtools) DOES reflow (unlike claude-in-chrome `resize_window`); push to `main` prints "pull request" advisory but SUCCEEDS (bypassed rule, ref updates). Parsing xlsx: `python3` + `openpyxl` present (read_only ws has no `.dimensions`). Persisted Supabase session lives in the `~/.chrome-debug-profile` — use `isolatedContext` for a clean logged-out check.
- **Tracker (`sprint-FlwnOI`):** `web-2b-r3`/`port-web-2a`/`port-web-2b`→in-review, `port-products-imagery`→done, `web-2b-r1` blocked. Browse-redesign + spaces-imagery + auth-nav shipped as refinements (not new tracker tasks).

### State as of 2026-07-02 (mobile nav drawer + launch prep + Sprint 5 CLOSED + Mark's Spex feedback — supersedes 6/30 "OPEN" items 1)

- **tablex-site `main` = `285d234`, prod ● Ready** (4 deploys today, all verified live). Dashboard `main` = `72b5592`.
- **MOBILE NAV DRAWER SHIPPED (`89ba7df`)** — the 6/30 open item #1 and last in-code launch blocker. `MobileNavDrawer.tsx` + shared `nav-links.ts`; SiteHeader hamburger stateful. `inert` when closed (a11y), scroll-lock w/ prior-value restore, matchMedia ≥900px auto-close, focus trap+restore, link-onClick close (NOT usePathname effect — React 19 lint). Cart omitted from drawer (dead stub); no CTA buttons (DS Don'ts). Desktop 1920+1280 unchanged. **Still OPEN from 6/30: cart icon→`#` and nav CTA buttons** (drawer shipped without both).
- **Launch checklist §2 DONE**: Payload DB password ROTATED (Management API; password never in transcript — lives in `.env.local` + Vercel encrypted env; prod `/admin` 200 verified); `PAYLOAD_SECRET`/`DATABASE_URI`/`RESEND_API_KEY` confirmed in prod. **Danny-manual remainder:** Adobe Fonts kit `juc1jwq` allowlist tablex.com+www & republish · Vercel Domains add (apex-primary decision) · legacy-WP afterlife. Then §3-5 DNS cutover (DON'T touch MX/M365) + `NEXT_PUBLIC_SITE_LAUNCHED` flip + redeploy + Supabase Auth site_url.
- **Sprint 5 `sprint-FlwnOI` CLOSED** (complete). `web-2b-r3`/`port-web-2a`/`port-web-2b` accepted→done w/ acceptance notes. Rolled to backlog pool (sprint_id NULL): `s4-mfg-decision` (Brian), `web-2b-r0` (locator data-blocked), `web-2b-r1` (news content-blocked), `web-2a-r1` (flagged possibly moot), `EW6cG9TI_r` (Caleb social). **`web-5-r3` stays in-progress, sprint-detached — the live launch arc.** No Sprint 6 created (Danny's call).
- **MARK'S SPEX FEEDBACK SHIPPED** (`acd494c` + `285d234`; scans at `docs/doc02638220260701112630.pdf` + `docs/Table X Knife Edge.pdf`; tracker `spex-mark-edge-fb` done):
  - **Edge offer 8→2**: `pvc-3mm` (3mm PVC, standard/DEFAULT) + `knife-edge` — BOTH repos (tablex-site `finishes.ts`, dashboard `finish-catalog.ts` `72b5592`; old list exists NOWHERE). Rulings in code headers: Phenolic = custom quote ONLY (profile ≈ 3mm PVC), Self Edge possible but NOT promoted (unlisted). Quote records store NAMES not ids → historical data safe; unknown ids fall back gracefully. `/finishes/edge-bands` rebuilt ("Two edges. Done right."); solid-surface pairings → quote-specified; "8 Edges" stat → "66 Laminates" (home/styleguide) / "2 edge profiles" (finishes hub).
  - **Twin bases**: procedural path renders 2 base clones at quarter points when rect/oval top len ≥ 1.5× depth. Native GLBs untouched (baked).
  - **Knife edge RENDERS in 3D** (`285d234`): `edgeTypeId` threaded into `SpexConfig`; procedural tops draw 0.512″ face + parametric 31.73° underside bevel (RUN derived from `TOP_THICKNESS_M` by formula), top height unchanged, degenerate guard on tiny tops. **Native combos = picker/spec-only** (edge baked in CAD); future option = fall back to parametric top when knife selected, NOT re-modeling files.
  - **Grain orientation = already correct, NO change** (scans landscape, grain along U → top's long axis; Mark's "NOT" render was a camera-orbit artifact).
- **SPEX RENDER-PATH GOTCHA (verification trap):** offers ≡ `MODEL_AVAILABILITY` cross-product → most literal-shape combos are NATIVE (bases baked in CAD — a native racetrack showing 2 bases proves nothing about our code). Procedural only fires in the **341 cross-product holes** (e.g. Foundation `RT|3660|TT`). **Always confirm the path via network**: `bases/*.glb` = procedural stand-in, `glb-models/…` = native.
- **3D-NEEDS BRIEF for the modeler: `docs/spex-3d-needs-2026-07-02.md`** (forward as-is). Coverage: 3,038 offered combos = 1,798 native + 389 procedural + **851 no-3D**. Asks: (1) 6 standalone base GLBs — T(227)/L(65)/Y(56)/DR(26)/QD(24)/X(10) → 408 combos; (2) six no-3D series (Revel/App/Element/Artisan/Solo/Trig) need offer LISTS first; (3) D-Shape(377)+Boat(61) → recommend CODE outlines, no files. NOT needed: knife-edge variants, tops, UVs/textures.
- **Dashboard configurator lint debt (pre-existing, not fixed):** `src/app/(dashboard)/configurator/page.tsx` has 3-4 `set-state-in-effect` eslint errors at HEAD — untouched by the edge cleanup.
- **Gotchas this session:** `vercel ls` status column needs the raw table (my grep-poll mis-parsed twice — use `vercel inspect <url>` or match deploy `created` time to `git log -1 --format=%cI`); pnpm strict store: `pg` unreachable top-level — require via `node_modules/.pnpm/pg@*/node_modules/pg` (CommonJS); foreground `sleep 100` is blocked — use `run_in_background` until-loops; a stray restored tab can appear in the reused headless profile (`/tmp/chrome-headless-nav`) — ignore.

### State as of 2026-07-04 (WEEKEND CLOUD RUN landed + reconciled — supersedes 7/02 "cart/nav-CTA open" items)

- **tablex-site `main` = `7bf4d7b`, prod ● Ready.** An autonomous cloud session (7/03, Danny-approved plan, direct-to-main) shipped 7 commits `674d1cd..907b2bd` + a CLAUDE.md status fix. **Canonical handoff doc: `tablex-site/docs/reports/2026-07-03-weekend-run.md` — REQUIRED READING before touching nav, footer, the Spex shell, or marketing copy.** Every push passed tsc+eslint+build+Playwright 1920/1280/390+banned-term grep; deploys READY-verified.
- **Shipped:** (1) **nav CTAs + cart resolved** (the 6/30 open items): bag icon → `/quote` ("Request a quote" aria), header pills *find your rep* (quiet) + *request a quote* (Ember, gated ≥1220px), drawer CTA rows. (2) **Spex polish:** 3D camera recenter (y 1.1, Bounds 1.35, mid-height target, Reset re-fits); `layout-constants.ts` + CSS vars replace the 78/76/154 magic numbers; two pre-existing overflow bugs fixed (toolbar actions over lead-time chip 1024–1190 → gated xl; SpecSheet footer wrap → `sm:basis-auto`). (3) **Full-site QA sweep** — all 66 sitemap URLs ×3 viewports + prod portal gating (seeded logins, airtight both directions, zero dollar figures) + interactive checks: wired footer sitemap (10 links) + home CTAs, de-linked `/ops/users` org names (pointed at never-built `/ops/orgs/[orgId]` = prod 404), copy honesty ("SKU, price" overpromise → "configuration code and spec"; Artisan "manufactured"→"mass-produced"; edge counts 8→2, 105→99; "Veranla"→"Veranda Teak"; Café diacritic; Quick Ship `$—` dropped; series big-stat clamp floor 160→96 fixing 390px horizontal scroll).
- **Punch-list note:** 2 of 4 Spex punch items (share-page 2-col parity; lead-time chip gating) were ALREADY shipped in `8cc313e` 7/02 — the run verified instead of rewriting.
- **NEEDS-DANNY LIST (parked deliberately — don't re-litigate autonomously):** 8 dead `#` links (footer Accessories; 3 socials; home watch-demo; home "see how quoting works"; Browse-by-Space Workplace + Outdoor rows) · "9,000+ Configurable SKUs" wording on ~6 surfaces · Artisan "mass-produced" voice call · space-detail template chips (conference-meeting chips contradict its hub copy) · "welded" chips (Stretch/Elite) vs Brian intent · `/ops/orgs/[orgId]` real page? · `/find-rep/[slug]` placeholder (guessable on prod — leave/noindex/404?) · rep-role portal flows unverified (no rep credential; Test Rep rep@example.com exists).
- **Tracker:** run added 3 done rows (`web-nav-ctas-0703`, `web-spex-polish-0703`, `web-qa-sweep-0703`, deliverable `web-3`) — I detached them from closed `sprint-FlwnOI` (7/04) to match the post-close convention. Remaining open work unchanged: `web-5-r3` launch (in-progress) + human-gated/future backlog (mfg decision, locator data, news content, portal E2E, Xero arc).
- **Cloud-session gotchas (recorded in the report §environment):** Playwright TLS needs proxy+route.fetch pattern; `NODE_USE_ENV_PROXY=1` for SSR fetch; Workflow tool permission dialog can't be approved unattended (use Agent tool); hard kill corrupts Turbopack cache (`rm -rf .next`); NO prettier config — never `prettier --write` (house ~100-col style); share mint against prod only. Test residue: share token `529b3f19e81b` expires Oct 1.
- **Launch position unchanged:** in-code work DONE; robots still `Disallow: /` (pre-launch guard active). Remaining = Danny (Typekit `juc1jwq` allowlist · Vercel Domains · legacy-WP afterlife · needs-Danny list above) + Brian (sign-off, mfg decision, territory data, news content) → DNS cutover + `NEXT_PUBLIC_SITE_LAUNCHED` flip + Supabase auth site_url.
